# DECISION LOG — TESTIMONIAL SYSTEM

**Source of truth: this file, in this repo (`F4LA/testimonial-system`, branch `main`).** The Google Doc in Drive is frozen (D-018).

What this is: the shared memory of the project. Every decision made anywhere (a Claude chat, a call, Slack) gets one row here. Every work chat in this Project reads this file from the repo at chat start and pushes new rows at chat close.

## Rules (read once, then just follow the format)

- Append-only. Never edit or delete a past row. If a decision changes, add a NEW row and mark the old one "Replaced by D-XXX" in its Status column. This is the only edit allowed on old rows: the Status column may be updated to record a later outcome (Replaced by D-XXX, validated live, superseded). Decision and Context are never edited or deleted.
- New rows go at the TOP of the table.
- One row per decision. Short. If it needs a paragraph of context, link to it — don't write it here.
- Capture at chat close. Every work chat in this Project ends with Claude committing the new row(s) directly to this file.
- IDs are sequential (D-001, D-002…). Check the top row for the last used ID.

## Status values

- Active — decision stands
- Open — decision pending (waiting on info, a spike, or a conversation)
- Replaced by D-XXX — superseded

## The Log

| ID | Date | Decision | Context (2–3 lines max) | Who | Status |
| --- | --- | --- | --- | --- | --- |
| D-101 | Aug 9 | PROCESS DECISION — the append-only rule is ABSOLUTE, and the Rules section now says exactly what it permits. The rule previously read "add a NEW row and mark the old one 'Replaced by D-XXX' in its Status column. This is the only edit allowed on old rows", which literally permits only one Status value and left every other after-the-fact update undefined. It now reads: "This is the only edit allowed on old rows: the Status column may be updated to record a later outcome (Replaced by D-XXX, validated live, superseded). Decision and Context are never edited or deleted." WHAT PROMPTED IT: D-099 was fixed and then validated live, and the validation was written INTO the D-099 row's text. Bernardo caught it and reverted it — the row's Decision and Context were restored byte-identical to the commit that created them (verified by diff, not by eye) and the confirmation moved to Status. THE REASONING: the log records what was known WHEN, not a tidied-up version written with hindsight. A row edited after the fact stops being usable as evidence of what anyone knew at the time, which is the entire reason the log exists — the same instinct that makes the Event Log append-only (a mis-marked event is annotated, never rewritten). Widening the Status wording rather than narrowing practice was the right direction because the information is genuinely useful ON the old row: "was this ever confirmed?" is a question you ask while reading the row, not one you want to answer by scanning forward for a later row. APPLIES TO BOTH LOGS — it is one rule, not two. The dashboard repo's own DECISION-LOG.md now carries the same rule in its header, adapted to the fact that it has NO Status column: there, the single place for new information about an old entry is a NEW ENTRY at the top referencing the old one, and a past entry's text is never rewritten, edited or deleted. (That was already the practice — a past entry there had been restored byte-identical and the new information moved into a new entry — but the rule was not written down in the file, so nothing told the next reader.) Living documents are explicitly the OPPOSITE and should be rewritten in place: DASHBOARD-SYSTEM.md, project-brain.md, CLAUDE.md. | Recorded as its own row rather than as a silent edit to the Rules text, at Bernardo's instruction — a governance change that is not itself in the log is exactly the kind of undocumented drift the log exists to prevent. | Bernardo | Active |
| D-100 | Aug 9 | Raffle MONTH ASSIGNMENT designed and approved — RECORDED NOW, BUILT IN THE DRAW CHUNK (the button is a write). Two parts. (1) DEFAULT, automatic: a testimonial belongs to the month of its EARLIEST event for that (email, cycle) — cohort-by-entry, in practice "Nomination — logged", the moment Gaby logged it. The month itself is the existing `activeMonth` setting (YYYY-MM; blank = the current month, exactly as that setting's own note already defines it). Cohort-by-entry was chosen over "the month they qualified" because qualification is UNSTABLE under latest-wins: a client who qualifies in August and resubmits the preferences form in September would silently hop cohorts and vanish from August's list, possibly after the draw already ran. Cohort-by-entry is fixed the moment a testimonial exists and matches the monthly nomination rhythm already in production (D-076). NOTE: `cycle` is NOT a calendar concept — it is 1, 2, 3 per client (part 1 vs part 2) and cannot scope a month; what it contributes is that a client's part-2 testimonial is a SEPARATE raffle subject. (2) MANUAL OVERRIDE, because a real operational case exists that the default cannot cover: a client says yes, does not send content that week, and sends it two weeks later — today the team decides by hand "not this month's raffle, note them for next month's". So Gaby can MOVE a client to another month's raffle from the raffle view, and that move WRITES AN ATTRIBUTED EVENT — it must never be a decision that lives only in Gaby's head. Same "system proposes, human disposes" pattern as the calendar dates (D-096). The read-only compliance chunk already RESPECTS a "moved to month X" override if one exists, so the view is correct the moment the button ships and no recompute is needed. KNOWN LIMIT carried forward: the preferences bridge writes no cycle value (logEvent_ appends five columns, so column F is blank), and a blank cycle folds to 1 — so a client's CYCLE-2 preferences submission attaches to cycle 1, the wrong testimonial. Harmless at launch (everyone is cycle 1), wrong on the first re-nomination. Deliberately NOT fixed inside a read-only chunk. STILL OPEN for the draw chunk: whether the draw runs among the month's cohort only, or among everyone currently qualifying who has not yet won — the cohort reading is being built for now. | Surfaced while building the Phase 5 raffle compliance view; Bernardo supplied the operational case the automatic rule missed. Recorded ahead of the build so the design is not carried in a chat. | Bernardo | Active |
| D-099 | Aug 9 | Preferences-bridge CLASSIFIER FIXED — amends D-098, which declared the bridge validated. The Google-review question's negative option is "Not yet", and the negative branch `/^n(o)?\b/` did NOT match it (no word boundary between the "o" and the "t"), so the commonest negative answer in the entire form was logged as `Unclear answer: "Not yet" — review manually` instead of a clean No — manual-review noise aimed at Gaby for a perfectly clear answer. It failed SAFE for the raffle (unclear is not a Yes, so nobody could wrongly qualify) but it was wrong, and it would have fired on every client who had not yet left a review. Fixed to `/^n(o\|ot)?\b/` and re-tested against all SEVEN of the live form's closed options — both photo yes-variants, the explicit photo no, both review options, both podcast options — every one now classifies, while genuine free text still flags for review. THE REAL LESSON: D-098's live validation used "Yes" on every question, so the negative branch was never exercised. A passing happy-path test is not coverage of the branch that matters; the option strings should have been read off the live form (which is what found this) rather than from a paraphrase. No schema change, no trigger change (a re-paste of the same file), proxy and PROXY_VERSION untouched. Deliverable: apps-script/engine-prefs-form-bridge.gs. | Found during Phase 5 raffle-compliance verification, by pulling the form's actual option strings instead of trusting the wording everyone assumed. The raffle view reads the raw answer preserved in the detail text, so it classifies correctly either way — but the LOG itself was wrong, and the log is the memory. | Bernardo | Active — validated live Aug 9 (Event Log row 89, "Not yet" → clean No) |
| D-098 | Aug 9 | Preferences-form → event-log BRIDGE built, validated live, and installed. Closes the data gap that would have made the Phase 5 raffle (spec §4.4) and reviews (§4.5) views read events that NEVER EXISTED — the same failure class as D-085 (routing correct, write silently absent): the client's own answers lived only inside the preferences responses sheet, nothing ever wrote them to the event log, so the raffle would have shown every client as "not qualified" forever. IMPLEMENTATION: onPrefsFormSubmit, installed as an onFormSubmit trigger on PREFS_FORM_SHEET_ID, mirroring onCoachFormSubmit (D-083). It writes through logEvent_ and DEPENDS on the SIGNAL_SHEET_ID fallback (D-085) because it is a NON-CONTAINER trigger — getActiveSpreadsheet() returns the responses file, which has no Event Log tab, so pre-D-085 every write would have vanished while the execution still reported Completed; checkPrefsFormWiring() asserts that fallback is present and says STOP if it is not. Answers are read BY HEADER, never by index (the columns already shifted once when D-097 added the podcast question). Email is the master key, resolved against the Active Client Roster (D-039); unresolved → a flag row written with an EMPTY email so it lands in the dashboard's system bucket instead of inventing a testimonial for a stranger's address — never guessed. FOUR event strings, all engine-owned and AUTO: "Preferences — photo permission" (raffle condition 1 + the client card), "Preferences — review self-reported" (raffle condition 3 + the reviews view), "Preferences — podcast consent" (the podcast chain ONLY, NOT a raffle condition, D-097), "Preferences — unresolved" (identity failure). The self-report deliberately does NOT reuse the dashboard-writable "Review — self-reported" string: keeping the client's own answers in an engine-owned Preferences group means a person CANNOT hand-enter a self-report and open the raffle, which makes D-066's never-merged rule STRUCTURAL rather than a convention. Raffle condition 2 (questionnaire/testimonial) reads the EXISTING client-video event — no new event was created for it. IDEMPOTENCY: none, by design — a resubmission appends three more events; the fold is latest-wins per (email, cycle, Stage), so the newest answer counts and the older ones stay visible as timeline history. Nothing is deduped, nothing is overwritten. ADDITIVE ONLY: no existing engine function modified, no event-log column renamed or reordered, and the write-proxy / PROXY_VERSION untouched (this path writes via logEvent_, not the proxy). Five triggers now. CARRY-FORWARD: "Preferences — unresolved" must surface as a GABY TASK in the alert engine (spec §5) when the alerts side of Phase 5 is built — today it is written but nobody is told. Deliverable: apps-script/engine-prefs-form-bridge.gs (591f813). | Validated live before close: a real submission for Cameron Colbo wrote rows 85-87 (photo permission / review self-reported / podcast consent, all "Yes") and resolved to the TYPED email, not the logged-in Google account — confirming email-as-master-key end to end. Built as Phase 5 groundwork, engine side only, 1 day before launch: additive by constraint. Cameron Colbo is pre-launch test data and joins the Aug 10 wipe. | Bernardo | Active |
| D-097 | Aug 9 | Podcast-consent question ADDED to the preferences form, closing the open item D-068 assumed captured but that never existed (flagged "to verify" in the Brain). The question ("Would you be open to being a guest on our podcast?") is a required Yes/No radio (structured, so the dashboard's podcast chain reads a clean value), placed in Section 1 before the Google-review branching so it stays out of the branch logic. Deliberately Yes/No with no "Maybe" and no follow-up path — a client is open or not, Bernardo does not want to chase it. Description kept NEUTRAL, with NO tie to the raffle. SEPARATELY, confirmed and enforced a boundary: the podcast is NOT a raffle entry condition. The raffle's three entry conditions remain exactly photo permission + questionnaire/testimonial + Google review (D-008, hard gate per D-059); tying raffle entry to podcast willingness was rejected as inventing a fourth condition and as unfair (it would pressure introverted clients or punish a "no" on a one-client-a-month opportunity — the raffle is for everyone who qualifies, D-048 keeps them separate). To make the three REAL conditions consistent, the form wording was aligned so each states it is part of raffle entry: the photo question already said it ("I won't be entered into the raffle"); the review question now carries "Leaving your review is one of the steps to be entered into this month's free-month raffle." Form (edit): https://docs.google.com/forms/d/1doPHm72qOQ7KcZG-XKE9YW-fVdI5j6HElrHon1-Qg38/edit | Surfaced as the one blocker Bernardo wanted closed before Phase 5 (the podcast chain assumes consent is captured). Done by hand in the form; the consent value now flows to the dashboard for the Phase 5 podcast chain. | Bernardo | Active |
| D-096 | Aug 9 | Dashboard Phase 4 (calendar + buffer) BUILT, validated, and live (commit 0de704a + fb107a0). Scheduling is BY WEEK, never by day (the dashboard neither knows nor shows days; which piece posts which day is SOP, one testimonial per week). TWO scheduling checks per testimonial — email + Instagram (Instagram covers reel/carousel/stories as one check) — both manual, independent, both required for a week to count as scheduled. BUFFER counts COMPLETE consecutive weeks ahead (complete = all 5 pieces done); target 4; dropping to 3 alerts Gaby. Two distinct metrics kept apart: OCCUPIED (a testimonial is assigned, complete or not) governs date assignment and prevents two testimonials colliding on one week; COMPLETE governs buffer health. An at-risk week (occupied but a piece pending) breaks the buffer streak rather than being skipped — so complete weeks stranded behind a gap are REPORTED, never COUNTED. On entering Scheduled the system proposes the first open week at the end of the queue respecting the buffer, computed in BATCH across all unassigned testimonials so no two collide without any anonymous write (Gaby's click writes it, attributed). Moving within Scheduled is a FREE move (no confirmation) but fires an active notice that the origin week is now empty + the buffer's fill suggestion (ready testimonial first, else oldest repost by last-used). Buffer strip communicates the fix, not just the number: it names the blocking testimonial + its missing pieces, shows how many ready weeks are dammed behind the gap, and what unblocking is worth (e.g. 2 -> 5) — so a healthy-looking number never misdirects Gaby into producing content she already has; only a genuinely empty queue tells anyone to produce. DATA: the scheduled week is stored in a NEW structured column G "Week" (ISO Monday, validated server-side), NOT parsed from free text — chosen because the buffer is the one computation duplicated in the digest and must read a clean value. This made the write-proxy change ADDITIVE (column G at the end, A-F untouched) and required PROXY_VERSION -> 4 (the bump is for column G, NOT new vocabulary — all Schedule stage strings were already in ALLOWED_STAGES since v1, confirmed live via a negative-control probe). Redeployed by editing the existing deployment ...qll5X-MnC3gZ (New version); setupPhase4() added the G1 header (83 existing rows keep a blank Week). Phase 4 retires the stopgap Schedule/Publish buttons on the client card (only state-builder.js still reads those stages, for the ladder). Scheduled->Published keeps its confirmation. Views: primary queue + secondary month toggle, one data source. FAST-FOLLOW (not Aug 10, deferred with D-088): the digest's server-side buffer number + buffer-low alert (carrying the fix/blockedBy text, not a bare number) + extending selfCheck to compare the buffer across both implementations — the buffer can drop two ways (a dashboard action the browser sees live, and a week rolling off the front only a time trigger catches), so the server-side number is not redundant. Deliverables: dashboard/calendar.js + the calendar views + the v4 proxy Code.gs. | Designed with Bernardo as strategic reviewer against build spec 4.3 (read verbatim) and the D-090/D-094 task model; the occupied-vs-complete split, the honest-buffer choice, structured column G over parsed text, and the buffer-strip misdirection fix were the real forks. | Bernardo | Active |
| D-095 | Aug 9 | Kickoff checklist split into TWO explicit steps, closing a gap that made Flow 3 (the video ladder) unreachable. Found while wiring the Everfit confirmation message: nothing anywhere wrote "Invite — instructions email sent", so the video clock could never start and no video follow-up task could ever appear. Now: Step 1 = Fire the fan-out (confirmed dialog — reaches outside the team); Step 2 = Send the instructions email + confirm on Everfit + mark it (SOP confirmation copy on a Copy button: "Hey [Name], I just sent you the instructions for your testimonial via email…"). The 48h video-check clock anchors on Step 2, NOT the fan-out — because Step 2 is when the client was actually told what to do; anchoring on the fan-out would chase someone never asked. Consequence to carry forward: all 8 existing test clients sit at "step 2 of 2" (fanned out before this step existed), so none has a running video clock until someone marks the email sent — correct behavior, but noted. Frontend-only, PROXY_VERSION stays 3. | Surfaced adding the SOP §3 Everfit confirmation message to the kickoff — the "Mark email sent" step described in the walkthrough didn't actually exist. | Bernardo | Active |
| D-094 | Aug 9 | Dashboard task/alert engine BUILT and VALIDATED to the v2 model (D-090), the last build piece before Phase 4. Declarative rule table: 7 flows, each row = owner + threshold + text + escalation, walked by an alerts engine; thresholds all in Settings (12 new keys seeded via syncSettings: outreachCoachNotMessagedHours, outreachReplyCheckHours, outreachFollowup1/2Hours, outreachCoachToldHours, videoCheckHours, coachFormFollowupHours, coachFormEscalateHours, contentCheckinDays, contentEscalateDays, approvalEscalateHours, coachFormUrl). CORE RULE enforced: every task belongs to a real dashboard user (Gaby/Miguel/Joey/Bernardo); COACHES ARE NEVER OWNERS — a coach not filling the form is Gaby's "chase the coach" task. Task text is the action, no stage jargon. All copy sourced: outreach follow-ups from SOP §2.5 (FU#1/#2 on the +24/+48h relative clock, FU#3 dropped — the deadline-anchored weekday model is superseded by v2's relative timing, a choice Bernardo made at v2 design); video follow-ups from SOP §3 (48h/48h/48h, matching Flow 3 exactly); tell-the-coach + coach-form follow-up are new v2 copy; all em-dashes removed. REFINEMENTS to the production half of D-090: (a) production clock starts at Producing entry (gate passed), not at video arrival; (b) content tracked PER CLIENT not per piece — one follow-up per client, 5d soft check-in to Miguel → 7d escalate to Gaby; landing-page-first is an internal Miguel/SOP matter, NOT a dashboard rule; (c) Everfit+photos get ONE soft escalation (passive reminder < collectingStaleHours/120h, stronger wording to Gaby past it — still Gaby, never Bernardo); (d) calendar/publishing generate no tasks yet (Phase 4). Also built: a URL-param SIMULATED CLOCK (?sim=+120h etc., preview-only, writes disabled) for testing escalations without waiting real hours — validated live (Everfit escalation rewrites at +120h, urgency recomputes). Grouping is now now/pending/review/overdue. WRITE-PROXY bumped to PROXY_VERSION 3 (adds 15 v2 stage strings to ALLOWED_STAGES + syncSettings) — server-side vocabulary validation would have refused every ladder button otherwise; redeployed on deployment …qll5X-MnC3gZ. requestFanout/Cameron path untouched. Deliverables: flows.js + alerts.js + the v3 proxy Code.gs. | Built with Bernardo as strategic reviewer against the v2 spec; the coach-owner routing, the SOP-copy conflicts, and the kickoff gap (D-095) were caught during validation. | Bernardo | Active |
| D-093 | Aug 9 | Pipeline move UX decided: BUTTONS now, drag-and-drop later; a defined taxonomy of confirmations vs hard blocks vs free moves; and forward-only (no undo) accepted as an open post-launch item. (1) BUTTONS are the working base for launch — labelled per-card buttons with confirmations, tested end to end. DRAG-AND-DROP is the planned enhancement layered on top AFTER launch (Bernardo prefers it as more intuitive; it does not replace the buttons, and the confirmation/block layer applies to a drop exactly as to a button — a drag is the easiest gesture to fire by accident, so the safety layer matters more with drag, not less). (2) MOVE TAXONOMY: three confirmation dialogs (Outreach→Invited [fires the fan-out — DMs a coach, shares folder 03 publicly], Scheduled→Published [hard to reverse], any→Declined/Dropped [removes from board, note required]); one hard block (Collecting→Producing per D-087 — disabled button that says what's missing, not an "are you sure"); everything else flows or is automatic. Rule: confirmations appear ONLY on outward-facing or hard-to-reverse moves, never on every move, or the operator learns to click through them. (3) NO-UNDO / FORWARD-ONLY: the log is append-only so no move can be reversed today; a "Pipeline — correction" event that voids a prior one is the right idea but out of scope 2 days before launch. Logged as an OPEN post-launch decision — the risk grows as the v2 model adds more one-click marks. Manual-add-to-Nominated was also built (pick from the Roster via dropdown, never free text; cycle 1 normally, max(cycle)+1 if prior testimonials exist, refuse if a prior cycle is still active). | Surfaced building the integration UI; Claude Code recommended buttons-only, Bernardo kept drag as a wanted later enhancement and scoped the confirmation taxonomy and the no-undo caveat. | Bernardo | Active |
| D-092 | Aug 9 | Dashboard write-proxy hardened after a silent-failure incident during integration testing. THREE fixes, all frontend/proxy, engine untouched: (1) the dashboard no longer uses no-cors, so it READS the server's real response and reports the actual error first (a write that fails now shows the exact reason instead of the button dimming and returning with no feedback); (2) action FEEDBACK moved next to the button plus a fixed on-screen toast visible regardless of scroll (the earlier build wrote the result to the bottom of the card, out of view — Bernardo had flagged missing feedback); (3) a PROXY_VERSION handshake — the deployed proxy reports its version and the dashboard shows a plain banner when the deployed code is older than the repo, so a "updated the code but didn't redeploy" mismatch warns loudly instead of failing silently (this is the same class of bug that hit the coach form; PROXY_VERSION=2 = adds requestFanout). Root cause of the incident: `requestFanout` existed in the repo but was never DEPLOYED to the live Web App — the standalone proxy has multiple accumulated deployments (repeated "New deployment" clicks create fresh URLs), and the wrong one had been updated; the dashboard calls one specific /exec (…qll5X-MnC3gZ) whose deployment was still on the Aug-7 version. Fixed by editing THAT deployment → New version. CLEANUP PENDING (post-launch, not blocking): archive the duplicate proxy deployments and rename the survivor "ACTIVE — used by dashboard/config.js"; the rule is always edit the existing deployment (pencil → New version), never "New deployment," which mints a new URL and leaves the old serving old code. | The kickoff fan-out failed silently in testing (button dimmed, no row written, no error). Diagnosed to no-cors hiding the "Unknown action" response + a stale deployment; fixed both plus added the version handshake so it can't recur unnoticed. | Bernardo | Active |
| D-091 | Aug 9 | Dashboard ↔ spreadsheet integration BUILT and VALIDATED end to end — closes the open piece from D-089, using Option C (polling), not the card-move-fires approach. HOW IT WORKS: the dashboard's explicit "Fire the kickoff fan-out" button (with a confirmation naming the 4 consequences — creates folder, shares folder 03 publicly, DMs the coach, copies matching Meet/Loom) writes a row into the engine's Signal sheet via the proxy (name in A, Confirmed=true in B, into the first empty pre-made row 13-30, never appended); a new engine time-trigger `processPendingSignals` (installed via `installSignalPollTrigger`, runs every minute) picks up confirmed-but-unprocessed rows and runs the existing fan-out. WHY Option C over A/B: onEdit triggers do NOT fire on script writes (so A — dashboard ticks the checkbox — can't work; confirmed by the well-established Apps Script limitation), and B (a new public engine endpoint) was too risky on the live engine 2 days before launch; C is purely additive (onSignalEdit and fanOut_ untouched, the manual checkbox still works identically as the free fallback), with three double-fire layers. The card move sets up the kickoff checklist; the explicit confirmed button — NOT a card drag — fires the automation, because it reaches outside the team. VALIDATED LIVE Aug 9 with a fresh test client (Cameron Colbo, coach = Bernardo): added to Nominated from the board → Outreach sent → kickoff fired → row landed in Signal row 13 → within a minute the fan-out ran (folder created, folder 03 shared + link surfaced to the card, coach DM sent, Meet note matched + copied) and the card moved Outreach → Invited on real events. Engine needs ZERO changes; the poll trigger is the only addition. Deliverable = `apps-script/engine-signal-poll.gs` appended to the engine's Code.gs + `installSignalPollTrigger` run (4 triggers now: onSignalEdit, sendMonthlyNominationMessage, onCoachFormSubmit, processPendingSignals). | The foundational piece the v2 task model depends on (D-089/D-090). Cameron Colbo is pre-launch test data → joins the Aug 10 wipe. | Bernardo | Active |
| D-090 | Aug 8 | Dashboard task/alert model REDESIGNED (supersedes the Phase-3 alert engine as first built). Approved model: every task belongs to a real dashboard user (Gaby operator, Miguel content-only, Joey approval-only [temporary 3-4 months], Bernardo supervises/escalations-only); COACHES ARE NEVER TASK OWNERS — coach-dependent items surface as Gaby's "chase the coach" tasks. Timing is relative per client (hours/days since entering the step, read from Settings), with escalation ladders, not a single waiting→overdue flip. Task text states the action, never the technical stage name. To the client, the sender is always Bernardo (Gaby executes from his voice). SEVEN FLOWS: (1+2 integrated) Outreach — one task with button-driven paths (copy / mark sent / coach hasn't messaged→24h×2→Bernardo / no reply→48h+48h follow-ups→tell coach / yes→kickoff checklist); the outreach→confirmation gap is now filled (mark sent → +24h check for reply → paths). (3) Client video: 48h→FU#1, 48h→FU#2, 48h→tell coach (2 client follow-ups then the coach, no third client follow-up). (4) Coach form: 24h→Gaby follow-up, 24h→Bernardo (dashboard auto-detects arrival, already working). (5) Everfit+photos: no loop, Gaby does it herself. (6) Content (Miguel): landing-page-first per client as priority/direction (not a rigid block; clients arrive on different days), Miguel marks each landing page done, "all done→tell Gaby" manual for now, then soft check-in to Miguel at 5d and escalate to Gaby at 7d. (7) Joey approval: 48h→Gaby tells Bernardo→Bernardo nudges Joey (stage temporary). New client-facing copy approved (outreach FU#1/#2, tell-coach-no-accept, coach-form follow-up, video-not-uploaded), no em dashes; existing SOP video follow-ups reused. Full model = deliverable "Dashboard — Task Model v2 (FINAL)" in the project Drive folder. To build: Claude Code rebuilds the alert/task engine to this model before Phase 4. | Surfaced reviewing the Phase-3 queue with Bernardo: the built model used absolute hour-clocks and assigned a coach (Brent) as a task owner, neither of which matches how Gaby actually operates. Anchored to Gaby's real follow-up SOP (relative cadence). | Bernardo | Active (production-half detail refined by D-094) |
| D-089 | Aug 8 | Dashboard ↔ spreadsheet integration ADDED as a new build piece: moving a client card to Invited in the dashboard must fire the collection fan-out directly (today it fires only from Gaby ticking the Confirmed checkbox in the Signal Sheet), so Gaby operates entirely from the dashboard and never touches the sheet. The dashboard already writes via the Apps Script proxy; this extends that path to trigger what onSignalEdit needs, with double-fire protection. Consequence: the separate "add to automation" button is removed from the kickoff checklist — the card move IS the trigger. Build now if the day allows, with a plan-B fallback (Gaby keeps ticking the sheet checkbox, connect after) if it gets risky 2 days before launch. Not an Aug 10 blocker — the checkbox path still works. | Raised by Bernardo while designing the outreach→kickoff flow: the dashboard and the engine were never connected, so "start the automation" had no home in the dashboard. This is the foundational piece the task model depends on. | Bernardo | Delivered by D-091 |
| D-088 | Aug 8 | Two-source-of-truth guard for the dashboard. The Slack digest runs on a time trigger with no browser, so Digest.gs re-implements the dashboard's fold (stage computation, ordering, five-fan-out inference, four-state inputs, the Collecting gate, the alert rules) in Apps Script — a genuine second source of truth and the repo's largest maintenance hazard. Guard = two parts: (a) selfCheck() promoted to a scheduled job (~every 48h) that compares the two implementations and EMAILS BERNARDO on divergence, so drift is an alarm not a silent bug; (b) an explicit written instruction in CLAUDE.md and/or DASHBOARD-SYSTEM.md that this logic lives in TWO places and any rule change must be made in both. | Flagged honestly by Claude Code when it built the digest; Bernardo asked for an active guard (email alert) plus the written rule so a future session is reminded before touching either side. | Bernardo | Open |
| D-087 | Aug 8 | Collecting → Producing GATE defined. The button that advances a client to Producing ("Mark collection complete") is gated at the point of action (not in the fold, to keep the stage event-computed and monotonic — an event that "exists but doesn't take effect" or a stage that could regress on a re-flag are both rejected). Gate = (client video present) AND (Gaby's manual part — Everfit + photos — marked complete). AUTOMATIC-INPUT FLAGS NEVER BLOCK: Meet and Loom being flagged or missing must not gate the button, because a flag can simply mean "this input doesn't exist for this client" (e.g. no Looms) and would strand the client forever. The client video is the essential piece (no video, no testimonial) so it is a hard requirement; everything else automatic is bonus. When the button is disabled it says why ("waiting on client video" / "Everfit not yet marked"). Verified live against all 7 test testimonials (Benjamin's flagged Meet/Loom correctly absent from his blockers). | Claude Code proposed gating on "all inputs unflagged"; Bernardo scoped it correctly — automatic flags can be permanently unresolvable, so only the video + the human step gate advancement. | Bernardo | Active |
| D-086 | Aug 8 | Pipix and Sofi are OUT of the testimonial process; the REEL is now edited IN-HOUSE by Miguel. Reverts the reel portion of D-071 (reel edited by the external agency Pipix) and D-072 (Pipix's first-reels favor as the startup ramp). Consequence: Miguel now carries landing page + in-page case-study video + reel; the startup no longer depends on Pipix accelerating the first reels (that fragile dependency in D-072 is gone). Sofi is removed as a dashboard user. D-071 and D-072 reel/agency portions marked superseded by this row; the collaboration-post model and the reel-required publishing rule from D-072 still stand. | Bernardo's call, communicated in the task-model session; the in-house shift simplifies the ramp and removes an external dependency before launch. | Bernardo | Active |
| D-085 | Aug 8 | Engine bug fixed — logEvent_ silent no-op on non-container triggers. The coach form routed to folder 04 but wrote NO event to the Event Log, so the dashboard (which reads the log, not Drive) would show the coach-form input as "missing" forever. Root cause: logEvent_ resolved the log tab via SpreadsheetApp.getActiveSpreadsheet(), which returns the WRONG sheet for form-submit triggers (bound to the responses sheet, not the engine container) and NULL for time-driven triggers — so getSheetByName returned null and the write was skipped with no error (Executions showed "Completed"). Fix: a fallback in logEvent_ that opens the engine by a new SIGNAL_SHEET_ID script property (= 17lWPi7o0Z1mR8yEkAh6vMEPOqZfQqSAaxeFM6eGIKmo, capital-I) whenever the ambient lookup fails; behavior-preserving for onSignalEdit and all fan-out pipes (they still resolve the active spreadsheet), and it also pre-fixes onClientVideoSubmit (if ever re-enabled) and sendMonthlyNominationMessage, which had the same latent bug. Also removed the one-time coach-form helper functions from the engine's Code.gs. Validated live Aug 8: a fresh coach-form submission (Benjamin Jayne) wrote "Collection — coach form → Response routed to folder 04" (log row 71) and turned the F input green on the card — the first engine-written event to drive the dashboard. Deliverable = full corrected Code.gs pasted into the bound Apps Script project. | Found when Bernardo's video-test flagged the coach-form input as missing on the dashboard despite the file being in folder 04. Same logEvent_ context class as earlier fixes; this was the first form-submit trigger actually in production. | Bernardo | Active |
| D-084 | Aug 8 | Testimonial Dashboard — Phases 1-3 BUILT and live (F4LA/testimonial-dashboard, GitHub Pages). Phase 1 (Foundation): read path, write path via Apps Script proxy, identity resolution (email→name→coach via roster, 0 unresolved), event-log fold, referrer-restricted read-only Sheets key (corrected to https://f4la.github.io/* — browsers strip the path from the cross-origin referrer, so the folder-scoped restriction could never match; same posture as Coach Pulse). Phase 2 (pipeline board + client card): 8-stage board sorted longest-in-stage, stage computed from events (never stored), the six corrected-fold fixes (Collecting entry = "Collection — video uploaded" OR "Collection — client video"; coach-form reads the engine's "Collection — coach form"; four-state inputs received/partial/flagged/missing mirroring the engine's own ✅⚠❌🚩; empty-email rows → System-events bucket; timestamps read as serials in America/Guayaquil not a display format; Invited inference locked to the five fan-out strings only), client card with the five spec blocks + direct folder-03 link. Phase 3 (action queue + alerts): per-owner queue (opens on the queue, not the board), thresholds from Settings, manual-review flags as tasks, the folder-03 "check for uploads" task in Gaby's queue scoped to Invited (Option A — the manual half of Invited→Collecting; Option C, an automated folder-03 poll running as "AUTO - dashboard", deferred to post-launch). Key design confirmations logged along the way: the client video reaches folder 03 by DIRECT UPLOAD, not a form — onClientVideoSubmit and its CLIENT_FORM_* properties are DEAD CODE from the abandoned form approach (per D-059/D-063/D-065); the engine writes NINE stage strings (five fan-out + client video + coach form + Confirmation + Nomination), all in the dashboard's never-write guard. Task 26 stays OPEN (closes when the dashboard is operational). NOTE: the Phase-3 alert model is superseded by the redesign in D-090 before Phase 4. | Built with Bernardo as strategic reviewer against the D-079 spec across the session; the detours (dead client-video code, the coach-form gap, the logEvent_ bug) were caught and resolved as part of getting the Invited→Collecting transition right. | Bernardo | Active |
| D-083 | Aug 7 | Coach recognition form BUILT and wired — closes the gap where the coach questionnaire content was approved (D-046) but the form itself was never created (D-063 built only the client preferences form; the collection engine already had the routing + the Slack notice waiting on two unset properties). WHAT: one general Google Form (the "04" input) built via a standalone Apps Script builder, mirroring the client-form pattern. "Client" is a dropdown fed from the Active Client Roster (exact canonical names), refreshed daily by a time trigger so a newly nominated client always appears and the coach picks — never types (identity discipline, D-039). The 3 approved recognition questions follow; coach email auto-collected as an audit trail (question 3 "do not publish" left optional). The engine's onCoachFormSubmit translates the picked name → email → the client's "04" folder and writes a Coach form markdown file; the coach's Slack notice links to this form. WIRING: two engine Script Properties set — COACH_FORM_URL (published responder link, used in the DM) and COACH_FORM_SHEET_ID (responses sheet, installs the onFormSubmit trigger) — then Install triggers re-run so onCoachFormSubmit binds. Validated end-to-end Aug 7 (Houri Esmaeilkhanian): DM carried the correct link, form submit landed "Coach form — ….md" in "04" with the 3 answers + coach email, and the event logged "Response routed to folder 04". Deliverables: Form (edit) https://docs.google.com/forms/d/1f0BcFAdAABWM2d6F6AMHzQk2NfoEhRJ6UL8Mn5He3Ek/edit · Form (responder) https://docs.google.com/forms/d/e/1FAIpQLSdSrP7-crZYsQ8DjDBgJUc06ojFRzz1pipduj3MUJue5jinwQ/viewform · Responses sheet id 1f0AKAK-UUOSZiDsNjfRAnI6vFGI0ITA6_tX1aqImDlE. Launch-relevant: without this the coach notice would have thrown on the first confirmed client (COACH_FORM_URL was pointing at the old flag-system form). Blocks no one downstream now → no Slack. | Surfaced while building the dashboard: Bernardo noticed the coach form had never been created. Gap sat between the approved questionnaire (D-046), the collection build that only wired routing (D-043), and pre-launch prep that built only the client form (D-063). | Bernardo | Active |
| D-082 | Aug 7 | PROCESS: sales/discovery and kickoff calls must be scheduled as a CALENDAR INVITE with the client added as a guest — not started as an instant/ad-hoc Meet. Grounded in a real case (Laurel Adamson): her discovery (Deniz) and a second call were instant Meets titled "Meeting started …", so the Gemini note had NO "Invited" line and NO client email at all — only her name in the body. The engine matches on the client email (master key, D-039/D-052) and never guesses by name, so email-less notes can't auto-attach; they flag for manual review (now actionable, D-081). Scheduling the call as a calendar invite makes Gemini capture the attendee email, so these calls attach automatically. Team-behavior change for the CLOSERS (Joey, Deniz, future closers), communicated by Bernardo — not a code change. Historical ad-hoc calls stay in the manual-review path. | Surfaced Aug 7 validating sales-call collection; normally the call IS agended, Laurel's was the exception (Meet created from scratch after an issue). Folds into the coach/closer non-negotiables briefing (before Aug 10) and the per-role SOPs (task 23). | Bernardo | Active |
| D-081 | Aug 7 | Collection engine — Meet retrieval hardened and its reporting corrected; validated in production Aug 7 across six clients. Identity rule unchanged (email is the master key; never guess by name). FOUR changes: (1) EMAIL VERIFICATION now reads the doc's HTML export, not plain text — a resolved attendee on the Gemini "Invited" line renders as a person chip whose email the plain-text export DROPS (keeps only the display name); HTML keeps the mailto:, so the email is present; loose emails survive either way. (2) SEARCH now also covers SALES CLOSERS, not just team+coaches — new script property SALES_ACCOUNT_EMAILS (comma/newline list of closer Workspace emails; Deniz added) is unioned + deduped with the coach list, so discovery/sales calls in a closer's own "Meet Recordings" folder are found. (3) REPORTING corrected — success vs flag is decided by MATCHED, not COPIED: docs already copied on a prior run count as success ("already there"), not a false "0 matched"; docs deduped across accounts; a real copy error reported distinctly (❌). (4) ACTIONABLE FLAG — when candidates are found by name but none carry the email (e.g. an instant Meet with no invite), the engine still flags but drops a "Needs review — call recordings.md" file in 01 listing each candidate with a direct doc link + the account it came from, so a human confirms in seconds. Also folds "sales"/"closing" titles into the Discovery subfolder (classification only names the folder, never filters). Deliverable = full Code.gs pasted into the bound Apps Script project (canonical source), delivered as a download; refresh the Drive .gs.txt archive with this version. | Testing Benjamin Jayne showed a persistent "0 matched" flag; root-caused live — the docs DID match, but plain-text export dropped the person-chip email AND the flag message conflated "already copied (idempotent re-run)" with "nothing matched"; separately, closers run sales calls in their own folders the coach-only search never reached. Validated across six clients: copied-new (Karen/Amy/Tammera), already-there (Karen re-run, no false flag), Loom valid (Amy) + no-transcript (Karen/Tammera), real 0-match with review file (Laurel). | Bernardo | Active |
| D-080 | Aug 6 | Process decisions that surfaced while designing the dashboard (they change how the team operates and correct the old SOP — recorded here so they're findable independently of the dashboard; all feed the per-role SOPs, task 23). (1) RAFFLE WINNER'S FREE MONTH is added in the client MASTER SHEET (only Joey/Bernardo/Miguel have access), NOT the Retention Sheet, and it is MIGUEL'S task — not Gaby's, not Bernardo's; corrects the old SOP (§8.3). The dashboard hands Miguel the task with the note text; it never writes to the Master Sheet. (2) RAFFLE POST-DRAW TASKS RUN IN PARALLEL, no chaining: the month-add (Miguel) and the winner + non-winner messages (Gaby, existing Everfit templates) all fire at once; corrects the old SOP order (which sent the winner message only after the contract was updated). (3) PODCAST INVITATION for the client of the month: Gaby handles the coordination — she invites and sends a GoHighLevel self-booking calendar link, the client books themselves; Joey does NOT do the reach-out, he only sends a short personal note before the call. Chosen so the coordination doesn't pile up on Joey while keeping his personal touch where it has impact. (4) Reconfirmed: the CASE STUDY is produced for EVERY client (not a monthly prize); the client-of-the-month SHOUT-OUT exists as a step but its copy and posting account are undefined (see the follow-up tasks). | Came up while designing the raffle/podcast surfaces; some correct the legacy SOP, so they're logged as standalone process decisions, not buried in the dashboard spec. | Bernardo | Active |
| D-079 | Aug 6 | Testimonial Dashboard (task 26) — design approved and full build spec delivered; build starts now in its own repo. WHAT IT IS: one integrated tool replacing the Asana board + the multi-tab sheet, built as an action engine (the proven clarity-call pattern — the system tells each person what to do today, they mark it, that advances state), reading/writing everything from the append-only event log (no migration; identity = client email via the Active Client Roster). SIX SURFACES: (1) pipeline — one card per active testimonial across 8 stages (Nominated · Outreach · Invited · Collecting · Producing · Review · Scheduled · Published) plus a terminal Declined/Dropped with a required note; stage computed from events, never stored; publish lock = ALL pieces ready (the reel is one of five, NOT special — refines D-072's "never without the reel": all-pieces already implies the reel, so D-072's intent stands, just reframed); (2) client card — everything about one client in one place: header + cycle label (supports part-2 testimonials via email+cycle) + direct links (Drive folder / raw video / landing page), input checklist, production checklist (each piece has an owner + a paste-the-link-to-mark-done field that replaces the link creators used to drop in Asana; the agency is a dashboard user like everyone else), a timeline weaving auto events + human notes (a note is an event, not per-stage boxes), and recognitions; (3) calendar + buffer — the Scheduled→Published timeline, queue primary view + month secondary, one row per testimonial with two "scheduled" checks (post + email), system-proposed dates Gaby confirms, auto buffer with suggestion+dropdown fill; (4) raffle — auto compliance on the 3 conditions, manual draw, snapshot at draw, parallel post-draw tasks (see D-080); (5) reviews — self-report vs confirmed never merged, raffle opens on self-report, weekly verification pushed as a task; (6) podcast + client of the month — vote stays in Slack, dashboard shows candidates and someone marks the winner with one click that fires the chain (see D-080 for the invite mechanics); podcast is a task chain, not a pipeline stage. Cross-cutting action queue = daily per-owner Slack DM digest (each person their own "today", same mechanism as the nomination scheduler) + the testimonial-management channel for content-production coordination, NO email; manual-review flags (D-039) surface as tasks. STACK: own public repo F4LA/testimonial-dashboard, vanilla modular HTML/CSS/JS like Coach Pulse (no framework, no build step) on GitHub Pages; reads via the Google Sheets API with a referrer-restricted read-only key; writes via an Apps Script Web App proxy; Slack via an Apps Script time trigger; secrets only in Apps Script properties. DATA: the existing "Testimonial Collection — Signal & Event Log" sheet, additive only (new "detail" column, a "cycle" value, new event types per phase; never rename/reorder — the live engine launches Aug 10). BUILD: 5 phases (Foundation · Pipeline+card · Action queue+alerts · Calendar+buffer · Recognitions); phases 1–3 = the usable core. OUT: aggregate metrics, published-content performance. Deliverable: "Testimonial Dashboard — Build Specification" → https://docs.google.com/document/d/1pbVibzWtkX0ZdETd4SWDabqAzYZO5P5x9-2OUH1U790/edit. Task 26 stays OPEN (spec is a milestone; closes when the dashboard is operational). Blocks no one → no Slack. | Bernardo consolidated and approved the whole design in one session; build starts immediately in its own repo, mirroring 21DC/Coach Pulse. | Bernardo | Active |
| D-078 | Aug 6 | Task 25 (wire the case study + weekly email into the storytelling agent) closed. The agent's operating instructions were rewritten as a new version (v3, supersedes v2/D-043) rather than edited in place (no Drive update tool). Two new outputs added to the same single run that already produces the carousel/reel/story: the case study (the full 8-beat body, then mapped directly into the landing page template's placeholders from task 22/D-077) and the weekly "Client of the Week" email (subject, hook, story tease, one result, primary CTA to the case study). Consent gates from the client's preferences form (credit name, photo permission, do-not-publish notes) are applied before filling any case-study element — missing permission is treated as "no," never assumed "yes." The case study's 2-source/2-voice quality bar from the template is checked before calling the beat done. Nothing publishes automatically — same AI+OK approval gate as before. Old v2 document left untouched in Drive (cannot be deleted or edited in place); its supersession is recorded in v3's own change history instead. Deliverable: "Storytelling Agent - Operating Instructions (cloud-native, v3)" → https://docs.google.com/document/d/19xSksweZDDDK5KDSs7dzVVq18OISsQjUTK6K2CaHbgo/edit. Blocks no one else → no Slack. | Feeds Miguel's own next task (validating the agent on the first real client, task 19/D-069) once that testimonial lands. | Miguel | Active |
| D-077 | Aug 6 | Task 22 (case study landing page design) closed — reusable landing-page template built for Strong Standard branding. Used Axel Rubio's already-live case study page as the reference (same brand system: navy/steel palette, Bebas Neue + DM Serif Display + DM Sans fonts), turned into a template with placeholders for every piece of client-specific content (headline, body copy, results, photos, video, quote). Locked the CTA count at exactly 3 buttons — sticky nav header, right after "Transformation at a Glance" (Results), and right after "Reflection" (closes the 8-beat arc) — removing the hero button and the Key Takeaways button that existed on the Axel reference page, to match the CTA-placement rule already on record. Delivered as a live HTML file in the Drive project folder; the Drive connector accepted the full write this time, no download-workaround needed. Deliverable: "Case Study Landing Page — Template (Strong Standard)" → https://drive.google.com/file/d/1qUl-GKRFfvQuh6E9ZjFUyTiTXCmaKxPg/view. Note: an earlier placeholder-only test write to the same folder needs manual deletion by a human (Claude cannot delete files) — https://drive.google.com/file/d/1T35qPPfZawmUIfy0glIqwZSE4vuXF3zj/view. Blocks no one → no Slack. | Miguel reviewed the Axel reference and confirmed the 3-CTA cut (sticky header + after Results + after Reflection). | Miguel | Active |
| D-076 | Aug 6 | Task 24 (T-A.7) closed — monthly nomination auto-scheduler built and validated in production. Hangs off the existing collection engine (same Apps Script bound to the Signal Sheet — reuses the Slack bot token, keyless auth, and the event log): a weekly Monday time trigger calls a function that computes the Monday nearest the 1st and posts ONLY on that Monday, so the weekly window never opens on a weekend; idempotent per month via a script property. Posts to the collection channel (C07TL4TMMTM) AS the "Testimonial Collection" bot and pings Gaby in a thread. Message = Gaby's existing copy verbatim, with one change: "Fit4Life Case Studies" → "Strong Standard Case Studies". Send time 5 AM, project TZ (America/Guayaquil). First AUTOMATIC send ≈ Aug 31 (September's nearest Monday); August's request goes out by hand as part of the Aug 10 launch. Posting as the bot (not impersonating Gaby): a user-token impersonation was rejected as fragile and unnecessary — the message names Gaby in the third person, so the bot reads naturally. Validated live Aug 6: test post landed in the channel + thread ping to Gaby confirmed. Deliverable = the full Code.gs pasted into the bound Apps Script project (same place the engine lives), delivered as a download. Rule implemented, not redesigned (process map, stage 1 / D-028). Independent task, blocks no one → no Slack. | Automates a manual monthly step at near-zero cost; not an Aug 10 blocker (August still goes out by hand), so worth closing without pressure. Hangs off the existing engine rather than a separate project because the Slack token, keyless auth, and event log are already there — a separate project would duplicate credentials for no gain. Posting as the bot (not impersonating Gaby) avoids a fragile user-token dependency, and reads fine since the message names Gaby in the third person. | Bernardo | Active |
| D-075 | Aug 6 | ROCK SCOPE EXPANDED — four items pulled into the rock, decided consciously by Bernardo (project director). (1) Auto-scheduling the monthly nomination message (nearest-Monday) → in scope, build owner Bernardo (was only deferred; not a reversal). (2) Wiring the case study + weekly email into the storytelling agent so it writes all five pieces from templates → in scope, build owner Miguel; REVERSES the "downstream / out-of-scope" framing of D-060 and D-067 (the templates themselves stand — only the scoping changes); makes explicit the always-intended design that the agent writes the case study from a template. (3) The DASHBOARD is built inside the rock, as soon as its blockers clear (start now if already clear); build owner Bernardo; the rock's close is NOT tied to the dashboard surviving a full cycle → REVERSES the "last phase / likely post-trip" posture of D-004 (and the "dashboard slots in after" posture of D-031); D-004 marked Replaced by D-075; the event log (D-034) already instruments the process, so no data migration. (4) The old backlog (~10 stored testimonials, D-032) is LOCKED as a pre-close gate: the rock does not close until the backlog decision is made; D-032 stays Open, decided once the first real client (D-069) shows what the new pipeline produces from complete inputs. Consequence: this documentation is no longer the penultimate task; the rock's finish line moves past Aug 10 (the Aug 10 launch itself is unaffected). New Master Plan tasks added (see plan); Asana task numbers pending. | Bernardo: we didn't finish before the trip, so expand the rock and finish the dashboard ASAP instead of pushing it to a separate phase; and the case study was always meant to come from the agent. | Bernardo | Active |
| D-074 | Aug 6 | Raffle conditions are communicated in the collection kickoff email (Stage 3.0), NOT in the outreach. The outreach may name the reward ("you can enter the free-month raffle") to lift the yes, but the actual entry requirements (photo permission, questionnaire, Google review) live in the email, read at the point of highest commitment; keeps one source of truth. Confirms current practice — the coach warm-up already tells the client Bernardo will email this week with the invitation + raffle details. | Bernardo asked for a recommendation, not just a description; this is the recommended and current design. | Bernardo | Active |
| D-073 | Aug 6 | Client-of-the-month vote window is 24 hours, not 48 (refines D-068). Everything else in D-068 stands. | Correction captured so the SOPs and the design doc agree. | Bernardo | Active |
| D-072 | Aug 6 | Publishing model + hard rules. Each format publishes ONCE, from Dr. Joey's page, as a COLLABORATION post with Strong Standard's page + the coach's page (surfaces on all three accounts from one publication — not three separate posts; corrects an in-chat misread). Coach adds one personal line only if they trained the client. HARD RULE: a testimonial never publishes without its finished reel; if the reel isn't ready, that week is paused and filled with the next available testimonial or a repost. Cadence: keep ~4 weeks scheduled ahead in steady state. STARTUP posture (August): only 1–2 publish in August by design, the rest seed September; the 4-week buffer is built, not demanded; a temporary favor to Pipix = first reels in ~5 days, sent in publication order, until 4 weeks are stocked — an explicit temporary exception, NOT a design dependency (if Pipix can't accelerate, the buffer just fills a week or two slower). Because the reel rule holds in startup too, the first publication waits on Pipix's first finished reel. Fine channel calendar + buffer mechanics live in the per-role SOPs (task 23). | Corrects the collaboration-post model and locks the reel-required rule. | Bernardo | Active (Pipix startup-ramp portion superseded by D-086) |
| D-071 | Aug 6 | Content production split consolidated (settles a recurring "who edits what" doubt; grounded in the Jul 3 leadership conversation + the current Content Creation SOP, not memory). Carousel + story: agent draft → Joey approves, in-house. Reel: the agent produces the edit list / timestamps from the client video; the EXTERNAL AGENCY (Pipix) edits the finished reel → Joey approves. Case study + landing page: agent drafts the text from the template; MIGUEL builds the landing page and edits the in-page case-study video → Joey approves. Weekly email: agent draft, links to the case study. The Jul 3 "we don't need Pipix" applied to the POSTS (Joey's Bobby carousel = 100% Claude) and never assigned the reel video — so no conflict; the reel stays with the agency. Pipix's wider role (content strategy, other formats) is outside this process. | Recorded so nobody re-litigates reel ownership again. | Bernardo | Active (reel/agency portion superseded by D-086) |
| D-070 | Aug 6 | Task 20 closed — master process design document delivered (T-E.3, Asana GID 1216566585460461). The final end-to-end design doc: 9 stages with all decisions integrated, an AI-first label + justification per stage, cross-cutting design (email-as-key identity, keyless delegation, append-only event log), and the who-does-what runbook (system / Gaby / coaches / Miguel / Joey / agency). Distinct from the per-role SOPs (task 23), which are the day-to-day operating docs built on top of this. Dangling-ref sweep done: process-map stage 3.5 "before/after photos — client uploads" corrected to Gaby's Everfit pull, client only grants permission (D-037); also folded every shift since the v0 map (Meet = Gemini-Notes harvest, not calendar attachments; review ask = link in the email body, QR out; case study for all, not "of the month"; launch Aug 3 → Aug 10; agent test on the first real client). English, plain language, no codes. Delivered as a download for Bernardo to upload (Drive connector aborts on large writes — same workaround as D-067). Deliverable: "Strong Standard — The Testimonial Process (master design document)" → https://docs.google.com/document/d/1ReX4xmUtK9NwzY92MbjUwzFl-tnWyeio-NA0SuUB7ok/edit | Master design doc; feeds the per-role SOPs (task 23). | Bernardo | Active |
| D-069 | Aug 5 | Real end-to-end test re-scoped: no separate recruited "trusted client." The new-standard validation runs on the FIRST REAL CLIENT of the launch cycle — the agent runs on that client's collected inputs and Joey approves before publishing; if it passes, the rest of the batch follows; if not, adjust and the content is still in hand. Rationale: the reason for the recruited test (D-049) — validate the new standard on complete inputs because the old backlog was thin — no longer needs a separate recruitment, since collection is already deployed and validated end-to-end in production (D-054/D-055) and the first real client IS the complete-inputs case. Approval-before-publish is the safety net → zero client risk (they get the normal kickoff email) and it yields the same evidence to decide the backlog (D-032 still open). Retires task 21 (Bernardo — recruit/feed the trusted client); re-scopes Miguel's agent run (task 19) from "the test client" to "the first real client of the cycle," starting when that first testimonial lands after the Aug 10 launch. Replaces the recruited-test premise of D-049 and the test item in T-B.6. | Bernardo's call: recruiting someone to redo a testimonial is pointless once real collection can start — test on client one; if it works, do all. | Bernardo | Active |
| D-068 | Aug 5 | Podcast criteria + client-of-the-month selection designed — closes task 11 (T-D.1, Asana GID 1216566617774631). Settles the two editorial pieces left open inside this task. CLIENT OF THE MONTH: chosen by an internal coach vote for the best story among the testimonials completed that month; one vote per coach; a coach may vote for their own client (recognition, not money — the free month is the separate raffle, so no conflict); NOT a community vote (keeps the "best transformation" comparison out of the community on purpose). What coaches review to vote, richest-first: the landing page once it exists → else the case study draft or the ≤2-page client brief → floor always available: the client video + coach form. Hard rule: the vote is never blocked waiting on the landing page or on Joey's approval. WHEN: the vote opens once the month's case studies are produced (after Miguel's production week), NOT at collection → lands at end of cycle; 48h window. HOW: in Slack, short candidate list, one pick each, most-voted wins. Ties: Joey (runs the podcast), Bernardo if Joey unavailable. Edge cases: single testimonial = client of the month by default; none complete = no client of the month that month; non-voting coaches don't block; unfinished testimonials aren't candidates. PODCAST: one episode/month, the client of the month is invited, criterion = the strongest/most inspiring story (not just numbers), prerequisite = podcast consent captured at collection. If the chosen client can't/won't do the podcast: stays client of the month (shout-out + case study); Joey invites the runner-up or skips that month; the vote is not re-run. TIMING tied to Miguel's production week (workflow detail → Miguel's role SOP, task 23): Miguel builds the landing page + the in-page video edit; the reel is edited by the external agency, so the vote does not depend on the reel. First August cycle: no landing page yet → coaches vote on case study drafts / the ≤2-page brief. Editorial, so it goes to Joey's async review packet (D-045). Not an Aug 10 blocker; unblocks no one → no Slack. Deliverable: "Client of the Month and Podcast — Selection and Invitation" → https://docs.google.com/document/d/11RR1xwRxNbEfDQHvGfa7n1sl8s4c519RjdatGld9w8I/edit | Inputs: client of the month via coach vote (D-048), case study for all (D-047), podcast ad-hoc per client + client brief (D-058/D-067), vote questions left open in the process map (D-028), Joey approves editorial async (D-045). Vote method + invitation criteria were the open items inside this task. | Bernardo | Active |
| D-067 | Aug 5 | Client brief (≤2 pages) designed — closes task 17 (T-D.2, Asana GID 1216566622837353). Single job: the human-readable summary of everything relevant about one testimonial client. NOT a prep sheet — Bernardo cut the podcast-prep layer (no possible questions, no "how to run the episode" guidance); Joey preps himself from it. Derived by COMPRESSION from the finished case study (not a fresh folder read — respects the reversed direction in D-060). Keeps: the factual arc + concrete data (starting situation/numbers → current results) + 2–3 verbatim client quotes. Drops: the case study's persuasive voice, its CTA, its source/evidence triangulation, and its length; neutral factual third person. Structure: one-line header (name as credited · coach · months in program · nomination month · headline result) + 5 blocks (Starting point · Challenges · What's working · Where they are now · Takeaways) + "In their words". Client named per the preferences form; do-not-publish content excluded. Left agent-compatible (deterministic from the case study) WITHOUT wiring it — wiring brief/case study into the storytelling agent stays a separate future build (D-060). Not an Aug 10 blocker; unblocks no one → no Slack. Drive connector failed on write again → delivered as a download for Bernardo to upload (known workaround). Deliverable: "Client Brief — Template & Usage" → https://docs.google.com/document/d/1-aQ-veAKNqyK4hql3K7C4DRLc5T3XpRq9aTxPRaX9_A/edit | Inputs: collected-inputs list (D-058), reversed direction + case study template (D-060), 8-beat arc (D-016), standard folder (D-037), questionnaires (D-046). | Bernardo | Active |
| D-066 | Aug 5 | Google review tracking designed — closes task 14 (T-C.2b, Asana GID 1216566617625377). Corrects an earlier framing: Google reviews are NOT anonymous — each review carries the reviewer's public profile name and photo — but Google gives no automatic match from a review to your customer record, so attribution is MANUAL (a person matches the displayed name to the roster; usually possible, sometimes not — nickname/initials/common name). Design tracks two signals kept strictly separate and never collapsed: client SELF-REPORT ("said yes", already captured by the preferences form) vs. human CONFIRMATION ("confirmed", a real review matched by name), plus the profile's aggregate review count over time as a reality check. Hard rule: the system never marks "confirmed" from a self-report. Raffle gate = self-report ("said yes"); confirmation is an AUDIT layer, not a gate (gating on confirmation would exclude genuine reviewers who can't be matched or whose review lands after the monthly cutoff; false negative worse than false positive at 3–5/mo). Status snapshotted at the raffle draw. Instrumented into the append-only event log (events: review requested / self-reported yes\|no / confirmed / "said yes but unmatched" flag); per-client state is a DERIVED view, consumable by the dashboard with no migration. Generic by design (keyed on email; testimonial cohort is one source of "requested"; a future full-base extension via the satisfaction questionnaire writes the SAME events, no redesign) — consistent with D-009; the full-base extension stays out of scope. Adds a best-effort weekly verification step to Gaby's process → feeds the SOPs by role (task 23) and final documentation (task 20). Build note: writing self-report + confirmation into the log is a small downstream build aligned with the already-planned form-response routing; NOT an Aug 10 blocker (launch review ask goes out in Gaby's manual kickoff email). Unblocks no one → no Slack. Deliverable: "Google Review Tracking — Design" → https://docs.google.com/document/d/1UERqXEEsxZa2kk_N99Nw4woTK8-UsYtm-beAhdbgwTA/edit | Verified live (Aug 5) against Bernardo's own review screen + web check: reviews show the reviewer's public name, no native review→customer match on Google's side. | Bernardo | Active |
| D-065 | Aug 5 | Client-video folder link auto-surfaced into the Signal sheet — closes T-A.6 (Asana GID 1217203594532990). On the fan-out that creates the client folder, the engine now (a) sets the "03 · Client video" subfolder to "anyone with the link – Editor" via a new shareClientVideoFolder_ helper, and (b) writes that subfolder's shareable link into a new Signal-sheet column E ("Client video link"), so Gaby copy-pastes it into the kickoff email instead of navigating Drive. Non-fatal by design: if the Workspace ever blocks external link sharing, the share/surface step logs a flag and Gaby shares 03 by hand (the documented manual path) — the rest of the fan-out is untouched. fanOut_ return changed from a string to { result, videoLink }; both callers (onSignalEdit → writes result to D and videoLink to E; manualTest → shows both) updated in the same pass. setup() adds the E1 header idempotently so re-running it on the live Signal tab doesn't rebuild it. No new script properties. Validated live in production (Aug 5): Manual test + real checkbox (Benjamin Jayne) both surfaced the link to col E, and the shared folder opens in incognito showing the upload dropzone — external client can upload with no login. NOT auto-send (the kickoff email lives in GoHighLevel — full auto-send stays a separate future project). | Retest evidence: incognito load of the 03 link shows "Drop files here / New" with no sign-in. Deployed by Bernardo in the Apps Script editor (canonical source). | Bernardo | Active |
| D-064 | Aug 5 | New task added to the Master Plan (T-A.6): auto-surface the client-video folder link into the Signal sheet. Owner Bernardo. On the fan-out that already creates the client folder, the engine also (a) sets the "03 · Client video" subfolder to "anyone with the link – Editor" and (b) writes that subfolder's shareable link into a Signal-sheet column, so Gaby copy-pastes it into the kickoff email instead of navigating Drive. Does NOT auto-send the email — full auto-send stays a separate future project (the email lives in GoHighLevel, not the engine). Not an Aug 10 blocker: the manual path (Gaby shares 03 + copies the link) is documented in the kickoff-email doc as the launch fallback. Its own chat: read the current engine code before specifying the insertion point. AI+OK. New Asana task, GID 1217203594532990 (numeric prefix pending alignment). | Raised by Bernardo in the pre-launch prep chat: removes the one manual navigate-share-copy step between the auto-created folder and Gaby's send. | Bernardo | Active |
| D-063 | Aug 5 | Pre-launch operational prep done (closes the operational tail of D-059). (1) Preferences form built in Google Forms via Apps Script to the approved spec, with TWO identity questions added at the top (full name + the email used with Strong Standard) so responses attribute to a client and join to the roster by email — email is the master key (D-039), name is the manual-review fallback when the email doesn't match. No-login verified in incognito (Collect email = Do not collect; Limit to 1 response off; no file-upload question). Q6 branches to two closing sections (Forms has one global confirmation, so the "leave your review" vs "thanks" message is built as answer-based sections). Responses sheet moved into the project folder. (2) Kickoff email wired: static published form link inserted; video link points to each client's "03 · Client video" subfolder (Gaby pastes it at send); internal send-steps added to the doc. Form (published): https://docs.google.com/forms/d/e/1FAIpQLSdKk3-u68UwfI-0ha9z4gL2Fn9Ms8wF_gj0cNEYa1EuxCBooQ/viewform · Form (edit): https://docs.google.com/forms/d/1doPHm72qOQ7KcZG-XKE9YW-fVdI5j6HElrHon1-Qg38/edit · Responses: https://docs.google.com/spreadsheets/d/1iXXrIxjZEoNGiLPW0p2OtK1E88z7MCqz_J1lVPyy_4Y/edit · Email doc: https://docs.google.com/document/d/1XYlt79ZNKBhNFt2lqxkKKp7WDU5CC22kFlSdYB-fI7c/edit | Identity questions are an addition to the approved form (Bernardo's call): the approved draft had no attribution field, which would have left responses anonymous and broken the hard raffle gate. | Bernardo | Active |
| D-062 | Aug 5 | New task added to the Master Plan: update the testimonial SOPs by role. Owner Bernardo. Distinct from the final process documentation (task 20 / T-E.3, the master design doc): this is the operational documentation people actually follow day to day — a plain end-to-end overview of how a testimonial works now, plus one SOP per role with only what that person does: Gaby (weekly process, Everfit pull, paste the folder link at send, mark the signal, review tracking), Miguel (agent/landing/reviews as applicable), coaches (the non-negotiables: Gemini note-taker every call, book via GoHighLevel, Loom link in the check-in description, keep the "Meet Recordings" folder name, name calls with the keyword). The coach non-negotiables that were floating in the Brain are folded into this task. Depends on task 20 (final documentation). AI+OK. New Asana task 23. | Raised by Bernardo: task 20 only mentioned "updates affected SOPs" as a side note, which underestimates the work of rewriting per-role operating docs for the new system. | Bernardo | Active |
| D-061 | Aug 5 | New task added to the Master Plan: design the case study landing page (Strong Standard branding). Owner Miguel. The reusable visual design of the page that hosts any case study — layout, typography, colors, and the 3 discovery-call CTA placements defined in task 10; a case study drops in, the design does not change. Built in Claude (already holds the Strong Standard brand guide + access to a landing-page project). Depends on task 10 (content/structure) + the brand guide. AI+OK — Miguel builds with assistance, a human approves. | Raised by Bernardo in the task-10 chat: content/structure and visual design are different jobs and belong in separate tasks. Adds a task to Miguel's column (WS-B). | Bernardo | Active |
| D-060 | Aug 5 | Case study + weekly email templates defined (closes task 10). Case study = long-form, evidence-rich account per client on a LANDING PAGE (not a PDF), built on the style guide's 8-beat arc with each beat triangulating ≥2 folder sources and ≥2 voices (client / coach / observed Loom evidence / data); the Method beat traces the journey over time (plateaus + mesocycle adjustments) so it reads as documented evidence, not a highlight reel. It is the lead magnet behind the "Comment [NAME]" social CTA. The landing page carries ONE CTA — book a discovery call — repeated in 3 placements (sticky header, after Results, after Reflection). The weekly "Client of the Week" email LINKS to the case study (not a short version), drafted for every client; distinct from client of the month (D-048). Direction of the client brief REVERSED vs D-058/T-D.2: the brief is derived FROM the finished case study, not an input to it. Agent produces copy + structure only (reel = edit list from the client video 03, a human cuts); wiring these two templates into the agent is a downstream build (agent today emits only the 3 social pieces, D-041/D-042). | Inputs: style guide (D-016), social templates (D-020), standard folder (D-037), questionnaires (D-046); case study for all (D-047). Landing page hosting + the discovery-call CTA are new info from Bernardo, not previously recorded. Deliverable: "Case Study + Weekly Email — Template & Usage" → https://docs.google.com/document/d/1ViWLdCNNyhY283mk48v4q7xxBmxtUdlm-nqb_6Z-dY0/edit | Bernardo | Active |
| D-059 | Aug 4 | Client instructions email ("collection kickoff") rewritten — closes task 13, the last design piece before launch. The single email a confirmed testimonial client receives: the approved 11-question video script (D-046); the video uploaded to the client's private Drive folder via a direct link (proven path — the fan-out already creates the folder on the "yes"); and a short no-login preferences form (no file-upload question, so it never forces a Google sign-in) capturing credit preference (full name / first name only), before/after photo permission (yes / yes-but-blur-my-face-in-the-photos / no), a do-not-publish note, and a Google-review self-report. Review link sits in the email body; QR left out (the email is read on the phone, where a tappable link beats a QR). Raffle entry is HARD: no photo permission or no review = not entered, though the testimonial still counts — makes D-008's expanded entry requirements explicit. The client never replies to the email; two surfaces only (folder + form). | Rewrite of the old GoHighLevel "Testimonial Email – Instructions" snippet. Video stays on the folder path because a Google Forms file-upload question forces a client Google login (confirmed in the engine code) — worse for the 40+/low-tech audience; the form carries only no-upload questions. For launch: Gaby pastes the auto-created folder link into the email (one paste); auto-send by the engine is the fast-follow. Form responses live in their responses sheet for launch; routing them into the client folder (mirroring the coach form → 04) is the clean fast-follow, neither blocks Aug 10. Deliverable: "Client Instructions Email + Preferences Form (collection kickoff)" → https://docs.google.com/document/d/1XYlt79ZNKBhNFt2lqxkKKp7WDU5CC22kFlSdYB-fI7c/edit | Bernardo | Active |
| D-058 | Aug 4 | Podcast episode outline dropped; replaced by a per-client CLIENT BRIEF (≤2 pages). Joey runs each podcast episode ad-hoc per client — no fixed outline needed. The brief summarizes everything collected: where the client started, how they felt, their challenges, where they are now, how they feel, what's helping them improve, and their takeaways (from the client's answers + coach input + call transcripts). It also feeds the case study and is the human-readable summary of the collected inputs. Re-scopes T-D.2 / Asana 17 ("Episode outline + integration" → "Client brief (≤2 pages)"). Master Plan change | Joey's creative call (relayed by Bernardo): a fixed outline adds no value when Joey adapts per client; a ≤2-page brief is more reusable (podcast + case study + a legible per-client summary) and maps to the 8-beat arc already defined (D-016). Downstream: the brief may become a storytelling-agent output — decided in its own chat | Bernardo | Active |
| D-057 | Aug 4 | Launch moves Mon Aug 3 → Mon Aug 10 (one-week slip; refines D-031). The collection engine finished build + validation on Aug 4 but without runway to start the first monthly cycle on the 3rd; Aug 10 is the next Monday. Everything else in D-031 stands (Sheet-signal architecture; dashboard slots in after). Meet now runs automatically (D-054), so manual-with-flag is only for genuine misses | The engine is validated and launch-ready, but a live monthly cycle still needs the client-instructions email (task 13) and a clean start; a one-week slip is the honest call | Bernardo | Active |
| D-056 | Aug 4 | Operator UX hardening: the code adds a "Testimonial System" menu to the Sheet (Setup / Install triggers / Manual test) and guards every getUi() call, so setup and tests run from the Sheet (where prompts/alerts work) instead of the Apps Script editor. No change to the pipelines | Setup threw "Cannot call getUi() from this context" when run from the editor; the menu is the idiomatic fix and lets a future English-only operator run everything without touching the editor | Bernardo | Active |
| D-055 | Aug 4 | Meet retrieval finalized (refines D-052). (a) The auto-save folder is matched by PREFIX "Meet Recordings*", not exact name — the AI Sales Coach team renamed coaches' folders (e.g. "Meet Recordings - Bernardo Sales Calls") while they still hold every call type. (b) Candidate Docs are found by client email OR client name (Drive full-text does not reliably index email strings; the Gemini Doc title carries the full name), then the client email is VERIFIED in the Doc body before copying, so name matches never cause false hits. Classification stays title-based (Discovery/Kickoff/End of Mesocycle → else Unclassified; classifies, never filters). Regression from the D-053 rewrite fixed in passing: renaming flags-header property keys orphaned the saved values, so FLAGS_HDR_NAME/FLAGS_HDR_DATE were re-aligned to the real headers ("Client","Timestamp") | Root-caused live from the per-account trace in the Event Log (7 accounts, 0 candidates → exact-name mismatch) and from the real Gemini Doc ("Invited" line carries the email; title carries the name). Downstream: reliable classification depends on call titles carrying the keyword — see the coach non-negotiables SOP | Bernardo | Active |
| D-054 | Aug 4 | English rewrite of the collection engine DEPLOYED and VALIDATED end-to-end in production. One clean pass: full English (tabs Signal / Event Log / Roster (mirror), per-client Collection status.md, Slack message, all code internals + function names); Slack DM wired to Coach Slack Email col J (D-051); Meet redesigned (D-052) to harvest Gemini Notes Docs from Drive. Trigger functions renamed (onSignalEdit / onClientVideoSubmit / onCoachFormSubmit) and reinstalled. Re-test (Benjamin Jayne, Bernardo's client): ✅ folder + template + status file · ✅ Meet (2 Gemini Notes copied, classified into 01/<type>, idempotent on re-run) · ✅ Loom (correct flag — no flag-form entry for this client) · ✅ Slack (DM delivered to the coach's app DM with the "Testimonial Collection" bot, not the person-to-person DM). Identity bridge, keyless signing, domain-wide delegation all green. Closes the retest pending in D-053; engine operational for launch | Evidence: Event Log ("2 Gemini notes copied from 4 matched docs") + client folder (01 · Call recordings → End of Mesocycle + Unclassified). Pre-launch test rows/folders were cleared afterward; permanent Event Log history starts at launch. Drive archive swap pending (new .gs.txt in, old fileId 1ZnL2hFldSyc_ZNUHNdtnZUgZeOJvdp26 out) | Bernardo | Active |
| D-053 | Jul 31 | Full English pass of the Testimonial Collection Engine, delivered as ONE clean rewrite that also (a) wires the coach DM to "Coach Slack Email" (D-051) and (b) implements the Meet redesign (D-052), then rebuilds the Sheet tabs (re-run setupInicial → English tabs), installs triggers, and re-tests with Pavle. Scope of the English pass: all operator-facing text (Sheet tab names, headers, per-client status file, Slack message) AND code internals (comments, function names), so a future English-only teammate or reviewer can operate/read it | Bernardo's call: future-proof for hiring and review; bundling avoids reopening verified code twice. Watch-outs for the rewrite: function-name renames must be mirrored in the trigger-creation strings and triggers reinstalled; script-property KEYS are bound to already-saved properties, so anglicizing keys means re-entering the saved properties | Bernardo | Active |
| D-052 | Jul 31 | Meet collection redesigned — realigns the build with the D-024 spike verdict (watch the "Meet Recordings" Drive folders), which the code had drifted from. The built code searched calendar events for attached recordings; the end-to-end test found 0 because Strong Standard's recordings are NOT attached to events — each call produces a "Notes by Gemini" Google Doc in the organizer's "Meet Recordings" Drive folder, containing the client email (line "Invited") + a Transcript tab. New method: via the verified keyless domain-wide delegation, search each coach's + membership@'s "Meet Recordings" folder for Gemini docs whose text contains the client's roster email; copy the whole doc (summary + transcript) into the client folder's subfolder "01", classified by title (Discovery/Kickoff/End of Mesocycle); no email match → flag, never guess. Captures the transcript (not the video) and decouples from the calendar | Root-caused live 31 Jul with Pavle Pavlovic. Depends on a coach non-negotiable (Gemini note-taker on every call). Supersedes the calendar-attachment approach inside the D-040/D-043 build; implemented in D-053 | Bernardo | Active |
| D-051 | Jul 31 | Coach identity is TWO emails, not one. Added a "Coach Slack Email" column (col J) to the roster: Bernardo/Ceci/Jackie = @strongstandard.com; Brent = brentmomb@gmail.com; Miguel/Joey = @fit4lifeacademy.health. "Coach Email" (Workspace) drives calendar/Drive delegation for Meet; "Coach Slack Email" drives the coach DM (used by avisarCoach_, wired in D-053) | End-to-end test exposed Slack users_not_found for brent@strongstandard.com — several coaches use non-Workspace emails in Slack. The system translates real identities rather than forcing account changes (same philosophy as the roster). Rejected: asking coaches to change their Slack email (fragile 3-person dependency before Aug 3) | Bernardo | Active |
| D-050 | Jul 31 | Roster enriched to carry the system's canonical identity fields, extending D-039. Added two array-formula columns to the Active Client Roster (tab "Roster"): "Client Name" (= First & " " & Last; matches the flag-Sheet "Client" names and feeds the Signal dropdown) and "Coach Email" (coach → @strongstandard.com Workspace email). The roster is a live QUERY from 'Mastersheet Data', so array formulas auto-extend and survive the daily refresh | Discovered at deployment: the real roster splits First/Last and had no coach email, so the collection code (expecting a single "Client Name" + "Coach Email") would have thrown. Translation stays in the roster (D-039), zero code change. Roster: https://docs.google.com/spreadsheets/d/1VxxqmOVuXffLOpPvMWnSUHhyhkjIajtBeBoSV3xk1fc | Bernardo | Active |
| D-049 | Jul 30 | Prueba del agente (test real end-to-end) redefinida: se recluta a un cliente de confianza que ya dio testimonio para que lo rehaga completo por el sistema NUEVO (se le manda el correo, su info entra por la tubería real, Miguel corre el agente). No se publica — es solo validación. Corre como ÚLTIMO paso, cuando todo lo demás esté construido. Bernardo recluta y alimenta; Miguel corre el agente. Reemplaza la premisa de usar el backlog viejo (D-032): los inputs del backlog son muy delgados —justo el problema que el cliente-de-confianza-con-inputs-completos resuelve— y de paso da evidencia real para decidir el backlog | Bernardo lo propuso; es más limpio que el backlog para probar el estándar nuevo. Depende de: cuestionarios finales (D-046) + recolección desplegada | Bernardo | Replaced by D-069 |
| D-048 | Jul 30 | "Cliente del mes" definido, distinto de otras dos cosas: (a) del ganador de la RIFA (al azar entre quienes cumplen requisitos → mes gratis, D-003/D-008), y (b) del caso de estudio para todos (D-047). El cliente del mes se elige por VOTO INTERNO de los coaches (la mejor historia), se le hace un shout-out público en la comunidad (se comparte ahí su testimonio) y se lo invita al podcast. El consentimiento de podcast se pide al recolectar la info. La función de lead magnet la puede cumplir cualquier caso de estudio (todos son completos). Se retira el término "caso de estudio del mes" | El método exacto de la votación/selección sigue abierto dentro de la tarea de podcast (tarea 11). Registrado con Joey | Bernardo | Active |
| D-047 | Jul 30 | Caso de estudio para TODOS los testimonios: RATIFICADO por Joey (estaba como default de trabajo pendiente de su visto bueno en D-036b). Caso de estudio idéntico para todos, sin "caso de estudio del mes" como artefacto diferenciado. El agente lo genera a costo casi cero; sin lógica condicional | Cierra el pendiente de ratificación de D-036b | Bernardo (aprobó Joey) | Active |
| D-046 | Jul 30 | Cuestionarios de cliente y coach APROBADOS tal cual (cierra tareas 05 y 06). Cliente: guion en video de 11 preguntas. Coach: formulario de 3 preguntas de reconocimiento. Desbloquea el correo de instrucciones (tarea 13) y fija el mapa pregunta→beat del agente (cierra el ítem abierto que arrastraban el spec y el build del agente). Entregable: doc "T-A.1 + T-A.2 — Client & Coach Questionnaires" → https://docs.google.com/document/d/1FxPx1DFTSCPw-KxmjOim9lUiwiIFdp4L7ML1ECbGgE4/edit | Aprobados por Bernardo/Joey; nunca se habían marcado formalmente | Bernardo (aprobó Joey) | Active |
| D-045 | Jul 30 | Todo el trabajo restante de Joey pasa a Bernardo como EJECUTOR; Joey aprueba solo lo creativo/estratégico (cuestionarios, caso de estudio, podcast) de forma asíncrona; lo operativo Bernardo lo cierra sin visto bueno de Joey. Afecta las tareas de Asana 10, 11, 13 y 17 (reasignadas a Bernardo; 05 y 06 ya quedaron aprobadas y completas). Cambio al Master Plan (owners). Los puntos que sí necesitan a Joey se juntan en un solo documento para su revisión asíncrona (largo del reel, método de selección del cliente del mes, y lo que salga del template de caso de estudio y del podcast) | Joey no tiene tiempo para trabajar en esto; se conversó y se decidió. Bernardo es project director | Bernardo | Active |
| D-044 | Jul 30 | Autenticación de la recolección SIN LLAVE (elegido sobre el método de llave JSON). El sistema actúa como las cuentas del dominio (delegación) sin archivo de llave: el proyecto de Apps Script se amarra al proyecto de Cloud "Testimonial System" (número 1039269378752), con la IAM Service Account Credentials API activada, y membership@ con el rol "Service Account Token Creator" sobre la cuenta de servicio testimonial-collector (109217142771648652059); el JWT delegado se firma vía signJwt. Se eligió porque la organización tiene una política que bloquea crear llaves de cuenta de servicio (iam.disableServiceAccountKeyCreation, versión managed + legacy, heredada a nivel organización); apagar dos políticas de seguridad de toda la organización se juzgó peor que un cambio de código localizado. VERIFICADO EN VIVO (30 jul: leyó el calendario de brent@ actuando como él, 5 eventos). El código se reconstruyó alrededor de este método; el camino de llave JSON queda descartado. Requisitos de despliegue documentados en el encabezado del código | Descubierto al intentar bajar la llave y toparse con la política de la org. Alternativa (apagar políticas) descartada por gobernanza | Bernardo | Active |
| D-043 | Jul 30 | Construcción del código de la recolección automática TERMINADA, sobre Google Apps Script amarrado al Sheet del disparador (plataforma confirmada; alternativa de servidor externo + polling descartada por sobredimensionada para 3–5 clientes/mes). Un solo proyecto: setup que construye el Sheet (señal con dropdown del roster + casilla, registro de eventos, espejo del roster), trigger de la casilla con anti doble disparo, carpeta desde template + archivo de estado, extracción de Meet (delegación, búsqueda hacia atrás por correo en membership@ y los coaches del roster, el nombre solo clasifica subcarpeta), extracción de Loom (puente correo→nombre completo vía roster, URL firmada fresca por video), aviso al coach por Slack, ruteo del video del cliente (03) y formulario del coach (04), banderas de identidad (D-039). Transcripción del video del cliente AUTOMÁTICA, colgada del link (si es Loom reusa la misma tubería) | Entregable: código "recoleccion-automatica (sin llave).gs" (856 líneas, sintaxis y lógica pura verificadas) — entregado a Bernardo por descarga en chat porque el conector de Drive abortaba en la escritura grande; PENDIENTE subirlo a la carpeta del proyecto en Drive (reemplaza el código viejo con llave). Guía de despliegue previa en la carpeta. Verificación en vivo completa del sistema entero = la prueba end-to-end (D-049). Asana task 16 ya estaba completa | Bernardo | Active |
| D-042 | Jul 17 | T-B.5 closed — Storytelling Agent (build) delivered per the T-B.4 spec (D-041). Built as an invocable Python script + two cached knowledge files (style guide, templates), run manually per client via Claude Code. Trigger, input, knowledge, and output match the approved spec exactly; question→beat mapping still pending Joey's questionnaire sign-off (unchanged open item). One build-time implementation choice made and flagged (not a project decision): the script reads a local copy of the client's folder rather than the Drive API directly, to avoid inventing new infrastructure outside the approved spec. Trigger-check and output-writing logic tested against synthetic fixtures; live generation against a real client is T-B.6. Unblocks T-B.6, same owner | Deliverable: "T-B.5 — Storytelling Agent (build)" → https://docs.google.com/document/d/1Nh31URcyenBRd4bR2CUS_jNtqL8iesYq-SYrsGcgnAc/edit . Working files (script + knowledge files + README) handed directly to Miguel — code isn't stored in the shared repo (scope limited to the three live docs, D-021) | Miguel | Active |
| D-041 | Jul 17 | T-B.4 closed — Storytelling Agent Spec v1 approved. Trigger: only on folder-complete status. Input: the 5 standard folder sources only, nothing invented. Knowledge: exactly the style guide + the 3 social templates (case study/weekly email excluded, Joey's separate deliverable). Output: all 3 social drafts (carousel, reel, story) in one coherent run, written into the client's content folder, AI+OK (human approves before publish). Runs as an invocable script via Claude Code, not a background service. One open item: question→beat mapping waits on Joey's final questionnaire approval — doesn't block the build. Unblocks T-B.5 (build), same owner | Deliverable: "T-B.4 — Storytelling Agent (spec)" → https://drive.google.com/file/d/1rWfPC76tfELzYwZ1NcsWflGndqVQXRbB/view | Miguel | Active |
| D-039 | Jul 16 | Decisión de identidad de todo el sistema: el correo del cliente es la llave única; el roster de planes activos es el índice canónico (correo + nombre + coach). Cuando un sistema de origen usa otra cosa, el roster traduce: (a) GoHighLevel/Meet empareja por correo del cliente en el evento; (b) el Sheet de flags usa NOMBRE, no correo, así que el roster convierte correo→nombre canónico y se hace match por nombre completo exacto (nunca por nombre de pila: hay nombres de pila repetidos, los completos son únicos). Si la traducción falla, el sistema NO adivina: levanta bandera de revisión manual en el archivo de estado. Aplica ~2% de casos (cliente con correo distinto en venta vs Everfit) y a los ~17 nombres del Sheet de flags que ya no están en el roster (churned) | Descubierto al revisar la data real (calendarios GoHighLevel, Sheet de flags con nombres, roster). Un falso positivo (colgar material de otro cliente) es peor que un vacío visible. El requisito "una llave consistente" ya estaba en E.1/E.2; esto confirma que hoy no se cumple en el origen y fija cómo se maneja | Bernardo | Active |
| D-040 | Jul 16 | Construcción de la recolección automática (spec v1) cerrada. Dispara con una casilla en un Sheet (señal desacoplada de Asana). Meet: búsqueda hacia atrás sin límite de fecha, por correo del cliente, en el Drive del equipo (discovery) y los Drives personales de los coaches (kickoff/end-of-meso); el nombre del calendario solo clasifica, no filtra; NO se ancla en lista de calendarios. Loom: índice = Sheet de flags, puente correo→nombre del roster, endpoint público (URL firmada, pedir fresca). Video del cliente y formulario del coach: solo se conecta el destino. Everfit sigue manual (Gaby, ahora con fotos), detalle en la SOP 3.6; el cliente solo da permiso. Recordatorio activo para Gaby: NO se construye contra Asana en el ciclo 1 (se integra al dashboard); en ciclo 1 el pendiente se anota a mano como hoy. Cada tubería escribe éxito/fallo/bandera en el archivo de estado; eventos en pestaña aparte. Construcción del código = tarea siguiente, de Bernardo | Entregable: "Construcción de la recolección automática — grabaciones + videos del coach (spec)". Entregable ya en la carpeta del proyecto: https://docs.google.com/document/d/1bLFk0SISNUKEhnnUAoQcAwDMVXA0EilQv6YHw1O-LD4/edit (excepción de conector cerrada). Reemplaza el borrador previo del spec de esta tarea | Bernardo | Active |
| D-038 | Jul 16 | Regla de comunicación: todo lo que lee un humano (chats con el equipo, entregables, manifiestos, nombres de carpeta, mensajes) va en lenguaje simple, sin códigos de tarea ni de decisión. Los códigos (T-x.x, D-xxx) solo viven en el Decision Log como índice interno. Se actualizan las instrucciones del Project para reflejarlo | Bernardo lo pidió varias veces: nadie del equipo memoriza los códigos y confunden. Registrado como decisión para que cada chat nuevo lo respete | Bernardo | Active |
| D-037 | Jul 16 | T-A.3 cerrado — Standard Client Folder v0 aprobado. Estructura idéntica por cliente, copiada de un template: carpeta nombrada "Nombre — email — mes de nominación"; email como clave de identidad visible. 5 inputs con lugar fijo (01 llamadas/Meet · 02 Looms · 03 video del cliente · 04 formulario del coach · 05 Everfit) + carpeta de contenido generado con slot para todos los assets (carrusel, reel, story, email semanal, caso de estudio) para TODOS los clientes (D-036b). Un archivo "Estado de recolección" por carpeta muestra qué llegó/qué falta con timestamp y registra fallos de los pipes AUTO (cubre salud del fan-out) — es la base que lee el event log y luego el dashboard. Sub-cambio importante: las fotos antes/después dejan de ser upload del cliente y pasan a ser parte del pull de Everfit que hace Gaby; el cliente solo da permiso (línea en el estado). El requisito de rifa se mantiene, cambia quién consigue la foto. Desbloquea T-B.4 (Miguel) y alimenta T-A.4 | Doc: "T-A.3 — Standard Client Folder (spec)" → https://docs.google.com/document/d/1uyOKQPS_AIZPhY9p60BbM-mSiXODhx9FOMglKrdM0OI/edit | Bernardo | Active |
| D-036 | Jul 16 | Avanzar sobre base tentativa: los cuestionarios draft (T-A.1 cliente 11 preguntas en video + T-A.2 coach 3 preguntas) quedan como BASE DE TRABAJO; el trabajo downstream avanza sin esperar el pase final de Joey. Tres precisiones: (a) T-A.3 se construye sobre la ESTRUCTURA del cuestionario (video→transcript del cliente + form del coach ruteados al folder), no sobre el wording. (b) Case study PARA TODOS los testimonios queda asumido como default de trabajo — B.3/B.4/T-D.1 se construyen sobre eso; el email semanal enlaza al case study en vez de ser una versión corta; pendiente ratificación formal de Joey (D-005, toca su template B.3). (c) T-B.4 (spec del agente, Miguel) puede arrancar tras el cierre de T-A.3 especificando todo MENOS el beat-mapping pregunta→beat, que se cierra cuando Joey apruebe el cuestionario final. T-A.1/T-A.2 NO se cierran; Asana 05/06 siguen abiertos; el doc en Drive sigue DRAFT | Joey revisó por encima ("menos genérico, más profundo") pero no tiene tiempo para el pase final por el viaje. Bernardo (project director) decide avanzar. El largo del reel (2-3 min) sale de la lista de bloqueos: es decisión de edición de última milla, no frena nada aguas arriba. Case study para todos es el caso MÁS simple de especificar (el agente siempre genera todos los assets del folder, sin condicionales) y con la misma data rica no tiene sentido degradar el email a versión corta. Doc: "T-A.1 + T-A.2 — Client & Coach Questionnaires (DRAFT for Joey)" en la carpeta del proyecto | Bernardo | Active |
| D-035 | Jul 15 | Cierre de tareas en Asana pasa a ser acción de Claude, no instrucción para la persona: al cerrar cada chat de tarea en este Project, Claude marca el/los task(s) correspondientes como completed directamente vía la herramienta de Asana. Si el task desbloquea el trabajo de otra persona (regla de handoff, D-023), Claude además envía el aviso por Slack a quien corresponde; si no desbloquea a nadie, solo se marca, sin aviso | Corrige un fallo real: D-028 (cierre de T-E.1) decía "Asana task 08 completed" como texto en el mensaje de cierre, pero nunca se ejecutó — quedó sin marcar hasta que Bernardo lo notó en el chat de T-E.2 y se corrigió manualmente (junto con el 07). Ejecutarlo como acción de la herramienta en vez de instrucción escrita elimina esta clase de fallo silencioso | Bernardo | Active |
| D-034 | Jul 15 | Instrumentación pre-dashboard: un **event log append-only**, separado por diseño de la señal D-029, ambos en el mismo Sheet. La señal-trigger (checkbox de Gaby, efímera, muere en el cutover) y el event log (una fila por evento: client_email · stage · timestamp · event · source AUTO/manual, histórico permanente) viven en pestañas distintas del Sheet de D-029 | Elegido sobre un Sheet único de estado mezclado: un event log append-only es el formato que CUALQUIER dashboard consume sin importar cómo se construya (Sheet, BD que importa histórico, o no-code). En el cutover solo cambia quién escribe el evento, nunca el formato — cero migración de histórico. Costo: una pestaña extra. Es también la estructura sobre la que corre el fallback de lanzamiento del 3 ago (D-031) | Bernardo | Active |
| D-033 | Jul 15 | **T-E.2 cerrado** — Dashboard Data Requirements v0 aprobado. Define, hacia atrás desde lo que mostrará el dashboard, qué registra cada una de las 9 etapas (E.1) y con qué captura (AUTO/manual). El dashboard absorbe las 5 funciones de Asana (pipeline, asignaciones, **historial de interacciones hoy escondido en comentarios**, loop de revisiones, recordatorios) y los 5 tabs del Sheet (Tracker, Calendar, Dashboard, Bank, Nominations+Raffle), más lo nuevo: checklist de collection expandida, salud del fan-out (D-029), compliance de rifa (D-008), tracking de reviews (D-009), pipeline de podcast, alertas y buffer calculado. Clave de identidad = correo vía Active Plan Roster. Performance de contenido publicado explícitamente FUERA (requiere APIs IG/email, no alimenta decisión del rock). Timestamps por etapa = dato día-1 de mayor valor y menor costo. Feeds T-A.3, T-A.4, T-C.2b, T-E.3 | Deliverable: "T-E.2 — Dashboard Data Requirements (Testimonial System)" → https://docs.google.com/document/d/1zv7MWLxrJhhq7iQyKbBs2QlFs07MWpEIYkPNKt1vOyE/edit . Nota: por fallo de escritura del conector de Drive, el doc se subió manualmente a la carpeta (excepción puntual a D-026, registrada) | Bernardo | Active |
| D-032 | Jul 15 | Backlog strategy (~10 stored testimonials): OPEN — resolved after T-B.6's result, in its own chat. B.6 doubles as the pilot for the decision (adapt with partial inputs vs. re-contact) | On record: Bernardo expects the old inputs (video + photos + Everfit only) to be too thin for the new production standard — B.6 is valid as a test, not as a production precedent. The backlog is also the content inventory to open August with a buffer | Bernardo | Open |
| D-031 | Jul 15 | Launch timeline set: everything except the dashboard closes Jul 17–19 (unchanged); Bernardo builds the dashboard the week of Jul 27; the full system launches Mon Aug 3 with the first monthly testimonial request on the new system. Fallback: if the dashboard slips, launch Aug 3 anyway on the Sheet-signal architecture (D-029) and the dashboard slots in after. Refines D-004: no production cutover — the system is born with the dashboard; Asana stays frozen as fallback | Aug 3 is exactly the nearest-Monday-to-the-1st date for August (D-028's nomination rule) — the first natural cycle of the new system | Bernardo | Active |
| D-030 | Jul 15 | T-B.3 scope extended: "case study template" → "case study + weekly client-of-the-week email template" (Joey). Master Plan change | Brief §4 lists the weekly email as a deliverable but no task covered its structure (today Miguel builds it per the old Content Creation SOP). Same chat, same creative work; the agent drafts the email once the template exists — the agent's DoD (3 social pieces) is untouched | Bernardo | Active |
| D-029 | Jul 15 | Confirmation trigger decoupled from Asana: the client's "yes" is signaled in a Sheet (Gaby marks a checkbox, ~10 sec); an AUTO fan-out listens to it — create the standard client folder + extract Looms (D-025) + copy Meet transcripts (D-024) + Slack DM to the coach with the questionnaire link. Nothing is built against the Asana pipeline (it dies with the dashboard); when the dashboard ships, only the writer of the signal changes | Dashboard-proof by design: zero rework at cutover, and it is the zero-cost fallback in D-031. Built in T-A.4 | Bernardo | Active |
| D-028 | Jul 15 | T-E.1 closed — Process Map v0 approved: 9 stages, AI-first labels, every manual justified. Map contents include: nearest-Monday rule for the monthly nomination message (target AUTO, implementation in T-A.4); review ask lives in the instructions email (collection kickoff) with only verification/tracking as a later stage; coach questionnaire = ONE general form with a client selector fed by the Active Plan Roster; full publishing inventory with scheduling mechanics as an open decision (resolves B.2→E.3); podcast vote questions (when/who/how) registered open → T-D.1 (Joey) with timing input noted | Deliverable: "T-E.1 — Process Map v0 (Testimonial System)" → https://docs.google.com/document/d/1bXEHkUWavEi281MuRDcgBDx_KjMhsBBEQiNiXGNMz1A/edit. Feeds T-A.3, T-A.4, T-C.2b, T-E.3 | Bernardo | Active |
| D-027 | Jul 14 | Reading rule hardened in the Project instructions: every chat MUST perform a genuine live fetch of the three raw URLs at chat start; the Project Files and the chat's own context are NEVER valid sources for the state of the log/brain/plan (they are frozen snapshots). If the top ID of the log looks stale or inconsistent with what the user says, re-fetch with cache-busting (code fetch with a no-cache query param) before assuming anything — a plain web fetch can return a proxy-cached old version even hours after a push | Root cause (Jul 14): one chat used stale state and believed D-022–D-025 didn't exist, blocking work; a second chat's genuine web fetch also returned a cached pre-push version and only a cache-busted fetch showed D-025 on top. The rule covers both failure modes | Bernardo | Active |
| D-026 | Jul 14 | Deliverables convention: every task deliverable lives as a single live Google Doc in the project folder in Drive (https://drive.google.com/drive/folders/1pxYhcm6o5e8T52cRK5KtGWt7t0V3WrJI). No duplicates in the Project Files (frozen snapshots) or in GitHub (which hosts only the three live Markdown docs). The chat creates the deliverable directly in that folder via the Drive connector — zero manual downloads/uploads. The Decision Log row of each task includes the deliverable's name + Drive link. Chats read deliverables live from Drive via the connector; nothing is "sent to a person" — everything leaves a trace in the log. Related convention: any chat proposing changes to hand-edited documents (e.g. the Project instructions) delivers the COMPLETE updated document ready to replace, never fragments | Intentionally replaces the earlier preference for locally delivered .docx files. First application — review-link deliverable, both files in the folder: doc "T-C.2a — Google Review Link (Strong Standard)" → https://docs.google.com/document/d/1o-JFgW4sE67N7v1mft7Xakm61M4a2tJqNkRlLzTghi0/edit and QR image "Google Review QR — Strong Standard.png" → https://drive.google.com/file/d/1LRFid6VDrQv1ndIMHuPysb1OgBhdmTa7/view | Bernardo | Active |
| D-025 | Jul 14 | Spike verdict — coach Looms: AUTOMATABLE, effort LOW — VERIFIED LIVE (Jul 14). Method: the flag-system Form/Sheet is the URL index (verified: 372 clean share URLs across 383 responses); a script fetches each transcript via Loom's public transcript endpoint (GraphQL fetchVideoTranscript → signed CloudFront VTT), share links, no auth. Live test on a real coach Loom returned a full WebVTT transcript. Impl note: the CDN URL is signed and expires — fetch fresh per extraction. Flagged risk: unofficial endpoint — fallback is manual copy from the Loom UI. SOP prerequisites: all coaches on the company Loom account (one to migrate) and the Loom field mandatory in the flag/minimum-standards forms | Closes T-0.1 together with D-024; unblocks T-A.4. Everfit stays out (D-007). Verdict doc: "T-0.1 — Spike Verdict" in the Drive project folder | Bernardo | Active |
| D-024 | Jul 14 | Spike verdict — Google Meet transcripts: AUTOMATABLE, effort MEDIUM. Method: service account with domain-wide delegation (one-time Admin Console setup) + a script watching the "Meet Recordings" Drive folders of the known organizer accounts (GoHighLevel sales + coach calendars), copying new transcript Docs into the standard client folder. Residual manual step accepted: enabling transcription in-meeting (current plan has no auto-record) — sales already records >95% as a non-negotiable; the same SOP rule extends to kickoff and end-of-mesocycle calls | Closes half of T-0.1; unblocks T-A.4. Meet REST API kept as an alternative if the agent later needs structured entries; Admin audit logs discarded (metadata only, no transcript content). Verdict doc: "T-0.1 — Spike Verdict" in the Drive project folder | Bernardo | Active |
| D-023 | Jul 14 | Master Plan cargado a Asana como proyecto de gestión del rock ("Testimonial System Rock — Master Plan", team Strong Standard): lista única de 20 tasks numerados en orden de ejecución (01–20), owner como assignee, due dates Jul 15–17, y el prerequisito en el título de cada task ("requiere NN"). Regla de handoff: todo task que desbloquea el trabajo de otra persona incluye en su descripción el paso final "al completar, avisa a [persona] y guarda el entregable en la carpeta del proyecto en Drive" | Ejecuta lo pendiente de D-011 ("tasks go into Asana"). El orden numerado + requisito visible hace obvio qué desbloquea qué; el aviso obligatorio elimina el handoff silencioso. D-022 sigue reservada (cuenta GitHub compartida, pendiente) | Bernardo | Active |
| D-022 | Jul 14 | Acceso a GitHub resuelto: el equipo usa una sola cuenta de empresa (membership@fit4lifeacademy.health) — mismo correo y contraseña para los tres, credencial guardada en cada máquina vía el setup de Claude Code (D-021). No se crean cuentas individuales de colaborador | Cierra el pendiente abierto en la migración. Trade-off aceptado conscientemente: los commits no distinguen autor por persona y la credencial es compartida; para 3 personas y 3 archivos Markdown, la fricción de cuentas individuales no se justifica | Bernardo | Active |
| D-021 | Jul 13 | Write path redesigned, replacing D-018's token model. **Reading:** Project chats read the three live docs via GitHub public raw URLs — no token, always latest. **Working:** all design stays in Project chats, one per task. **Writing:** done through Claude Code (one reusable session per person; GitHub credential lives on the machine from a one-time setup, never in chat text). The Project chat hands over the change CONTENT in clean text; the person passes it to Claude Code, which does pull → edit → push. A `CLAUDE.md` in the repo root instructs Claude Code to always pull before editing and push after, so no one has to remember the sequence. **Scope:** only the three live docs (decision-log, project-brain, master-plan) live in GitHub; everything else (Sheets tracker, Brief, SOPs, transcripts) stays where it is. The token is removed from the Project instructions | D-018's token-in-instructions model failed unpredictably: some chats (e.g. Joey's) refused to use a plaintext credential, others lacked code execution. Moving the credential to the machine via Claude Code removes both failure modes and keeps writes consistent for everyone. Reading was never the problem — public raw URLs already solved it. Full setup + instructions to be produced in the follow-up chat | Bernardo | Active |
| D-020 | Jul 13 | Per-page voice adaptation resolved (closes the open item from D-017): single asset reused across all pages; only the caption narrator shifts (Joey "I" → Strong Standard "we"); a coach may add one personal first-person line only if they personally trained that client (T-B.2) | Resolved by Joey in the T-B.2 chat as part of the social templates delivery. Templates: carousel (8-slide base + 9–10 expansion zone between Results and Reflection), reel (6-segment timing, client's own on-camera words favored), story (teaser/companion to the main post, zero new copy) | Joey | Active |
| D-019 | Jul 13 | Repo documents stay in pure Markdown — no HTML, no GitHub Pages | GitHub renders Markdown tables natively; no build step, readable diffs, works on private repos. Consumers are the team + Claude chats — nobody needs a website view | Bernardo | Active |
| D-018 | Jul 13 | GitHub repo `F4LA/testimonial-system` (branch `main`) becomes the single source of truth for the Decision Log, Project Brain and Master Plan (`decision-log.md`, `project-brain.md`, `master-plan.md`). Drive Google Docs frozen with a note pointing to the repo. Claude chats read the three files via the GitHub API at chat start and commit updates at chat close — no more manual pasting | Drive snapshots in the Project knowledge kept going stale and manual pasting was the main failure point of the chat workflow. Repo gives versioned history and closes the read/write loop | Bernardo | Replaced by D-021 |
| D-017 | Jul 13 | Publishing pages set: Dr. Joey's page (primary, first-person voice), Strong Standard's page, and potentially coaches' pages. Bernardo's page is NOT a publish target — updates the Brief §4 assumption | Voice of the style guide is anchored to Dr. Joey as narrator because his page is primary. Per-page voice adaptation resolves in T-B.2 | Joey | Active |
| D-016 | Jul 13 | Storytelling style guide v1.0 approved — closes T-B.1. Formula: 8-beat arc (hook→context→struggle→turning point→method→results→reflection→CTA) across 7–10 slides, expansion slides reserved for beyond-the-scale material (capability, relationships, freedom, identity); Dr. Joey first-person voice; pain-point hooks (never results); numbers as supporting evidence; banned-language list; agent sourcing rule (nothing invented); universal "Comment [KEYWORD]" CTA (client name → case study, or coaching keyword → conversation) | Derived from 8 published carousels incl. the Bobby experiment + Joey's direction. Doc in Drive: "Strong Standard — Storytelling Style Guide (T-B.1, v1.0)". Feeds T-B.2 and T-B.4 | Joey | Active |
| D-015 | Jul 10 | Joey's "physical component" idea (card/QR at a physical location) discarded for this rock: Strong Standard is registered as an online/service-area business with no customer-facing physical location (D-013), so there is no touchpoint where a physical artifact would be encountered. Revisit only if an in-person client event materializes — out of scope for this rock | No physical location exists to place a physical artifact; review capture stays fully digital | Miguel | Active |
| D-014 | Jul 10 | Google review capture mechanism: a single static direct link (Google's native share-review link, `g.page/r/.../review`) plus a QR generated from it, both derived from the GBP Place ID. Generated once GBP verification completes; embedded into the client instructions email (T-A.5) and other digital touchpoints | Fully automatable once Place ID exists; no ongoing maintenance needed | Miguel | Active |
| D-013 | 2026-07-10 | Google Business Profile created as an online/service-area business (no physical address displayed), registered under the name "Strong Standard". Submitted for Google's automatic verification; review pending (≤5 business days) | Strong Standard operates as remote/online coaching with no customer-facing physical location; service-area is the correct profile type for accurate local search behavior and to unblock T-C.2a and T-C.2b downstream | Miguel | Active |
| D-012 | Jul 9 | All collection reassigned to Bernardo end to end: the transcripts+Looms spike (T-0.1) and the collection build (T-A.4) are fused into single tasks, both his. Meet + Looms + Everfit = one owner. Replaces the Meet/Looms split in D-011 | Bernardo defines the standard folder (A.3), so having him own the pipes that fill it removes the plan's most tangled cross-person handoff. Miguel focuses on Google Reviews + the storytelling agent | Bernardo | Active |
| D-011 | Jul 9 | Master Plan v1 approved: 22 tasks (after a/b splits), owners (Joey 8 / Miguel 7 / Bernardo 7), dependencies and execution order. Spike and collection build split: Google Meet = Miguel, Looms = Bernardo — each builds what they investigated | Full layout in the Master Plan doc (Project knowledge). Fulfills D-002. Critical path: B.1→B.2→B.4→B.5→B.6 | Bernardo | Active |
| D-010 | Jul 9 | Process is instrumented for the dashboard from day 1: a one-page data-requirements doc (T-E.2) defines what every stage must register. Dashboard build stays post-trip | Avoids rebuilding everything later because stages didn't capture data. D-004 unchanged — this is the seed, not the build | Bernardo | Active |
| D-009 | Jul 9 | Review tracking is designed generic from the start (any client, not just testimonials). Extending review requests to the full client base via the satisfaction questionnaire is optional and OUTSIDE the rock's DoD | Costs the same to build it right once. The extension only happens if time remains before the trip | Bernardo | Active |
| D-008 | Jul 9 | Raffle ENTRY requirements expand: before/after photos + story questionnaire + Google review. The raffle draw logic itself (random, free month) is untouched | Direction set by Joey at sprint planning. Clarifies the boundary of D-003: entry requirements live in collection, not in raffle logic | Bernardo | Active |
| D-007 | Jul 9 | Everfit data collection stays manual (justified): Everfit has no open API. Removed from the technical spike's scope | Gaby keeps pulling metrics/visuals as today; documented as a justified manual step in T-A.4b | Bernardo | Active |
| D-006 | Jul 9 | Coach questionnaire will be built regardless of Loom/transcript access. Replaces D-001 | Bernardo changed position: even with Looms available, the coach's written input is wanted. Coaches still have no tasks in the rock | Bernardo | Active |
| D-005 | Jul 8 | No additional design session. Strategic direction was set at sprint planning; open decisions are resolved by each task's owner as work happens, logged here, with Joey approving creative/strategic items async | Small team, everyone loaded with recurring work. The project advances through assigned tasks + this log + Wednesday check-in + L10 — zero new meetings | Bernardo | Active |
| D-004 | Jul 8 | Dashboard is the last phase, likely post-trip. Direct cutover when built (no parallel run); Asana pipeline frozen — not deleted — as fallback until the dashboard survives one full cycle | Bugs only surface with real use; parallel running doubles work. Freezing Asana costs nothing and protects the 3–5/month flow | Bernardo | Replaced by D-075 |
| D-003 | Jul 8 | Outreach flow and monthly raffle logic are NOT redesigned in this rock — they get integrated into the new process design as-is | They work: 1/month → 3–5/month since January. Don't touch what produces | Bernardo | Active |
| D-002 | Jul 8 | Task assignments between Bernardo and Miguel are made during Phase 1 task planning, not pre-assigned | Both cover ops + tech; Bernardo directs the flow and assigns based on the actual task list | Bernardo | Active |
| D-001 | Jul 8 | Coaches' role in the new process (fill a story questionnaire vs. not needed at all): OPEN — depends on the technical spike on programmatic access to Looms + call transcripts | If Loom/transcript scraping works, the coach's side of the story may come from there and no questionnaire is needed. Coaches have no tasks in the rock either way | Bernardo | Replaced by D-006 |
